# 核心身体控制与蒙皮贯通

本里程碑的三组核心工作已接通。下列证据限定于标准合成拓扑、显式代理网格和当前姿态操作。

| 组 | 状态 | 交付与证据 |
| --- | --- | --- |
| 脊柱 IK/FK | 已实现并验证 | 固定 Root / Spine1 / Chest 双段机制、独立基部 FK、混合、腰部 roll、双向当前姿态匹配；30 / 70 关节 Maya 后台通过 |
| 头部与手脚控制空间 | 已实现并验证 | 5 组 / 9 个约束；头部朝向、手脚 goal 与 pole 成组切换；30 / 70 关节当前世界姿态保持，支持 Undo / Redo |
| 控制到蒙皮贯通 | 已实现并验证 | `examples/maya_complete_character.py`；30 / 70 关节、348 / 828 顶点；显式权重、全身动作、独立蒙皮方程核对、整体 Undo / Redo 和失败回滚 |

## 本批次证据

- 292 项纯 Python 回归通过。
- `maya_body_spine_smoke.py` 在 70 / 30 关节场景通过：直立及弯曲姿态双向匹配、0.5 混合位置插值、骨盆与腿部隔离、颈头 / 肩胛 / 手臂跟随、全局平移 / 旋转 / 1.5 倍缩放下匹配、锁定输入拒绝、注入匹配失败回滚、匹配和构建 Undo / Redo。
- FK→IK 全 Body 世界矩阵最大元素误差约 `1.0486e-5`；IK→FK 约 `6.22e-15`；全局变换下约 `2.8024e-6`。运行时验收阈值为 `1e-4`。
- `maya_body_control_spaces_smoke.py` 在 30 / 70 关节通过：身体与 Global 跟随差异、全部空间双向切换、整体构建 Undo / Redo、锁定拒绝、注入切换失败回滚、选择与时间保持。最大矩阵误差均约 `7.56e-14`。
- 原 Torso-only 30 关节 smoke 通过，旧 FK 调用保持兼容。
- `maya_complete_character_smoke.py`：双侧臂腿 FK / IK、躯干 FK / IK、头部、五指角色的双侧食指，以及 Global 平移 / 旋转 / 1.5 倍缩放均产生实际顶点位移。每个顶点按绑定逆矩阵、当前关节矩阵及显式权重独立计算，再与 Maya 结果比较；30 / 70 关节最大位置分量误差分别约 `5.17e-7` / `9.49e-7`。
- 显式权重读回误差为 `0`；脊柱匹配与全部空间切换后的网格最大位置分量误差约 `6.64e-5`，低于 `1e-4`。该网格误差与关节矩阵误差分别记录，不混用单位。
- 整个 Fit → Body → 控制 → 网格 → 蒙皮流程一次 Undo 移除、Redo 恢复；重做前后 Body / offset 矩阵及网格位置误差均为 `0`。在显式权重写入后注入异常，整个流程回滚；已有场景被拒绝且无新增节点。
- 独立角色空间 smoke 增加重做前后整个 Body 和全部 offset 矩阵核对，修复此前仅检查节点存在不足以判断 Redo 正确的问题。
- 本机详细结果位于忽略的 `validation/results/maya2024-body-spine*.json`、`maya2024-control-spaces*.json` 与 `maya2024-complete-character*.json`，不提交运行日志。

## 完成边界

脊柱使用固定长度双段 RP IK。完全伸直匹配时，IK 目标沿伸展方向外移总长度的 `1e-5`，让 Maya 求解器落在最大伸展位置；Body 姿态仍以同一阈值读回复检，不放宽标准。完全折叠导致起止点重合时，无法确定 bend plane，当前拒绝匹配。

匹配只作用当前姿态，不写关键帧；目标已有动画输入、锁定、引用、动画层、非等比 / 反射缩放和部分混合值会被拒绝。标准场景角度单位为 degree。尚不支持脊柱拉伸、任意脊椎数量或旧角色自动改造。

空间切换重建本工具拥有的单源约束，保留当前帧 offset 的世界姿态；约束上的动画 / 外部输入、外部消费、目标锁定等在写入前拒绝。该操作不写动画键，也不保持其他帧的历史运动。

代理网格只验证完整控制与显式蒙皮链路，不代表生产角色表面、自动高质量权重或真实资产适配。当前里程碑不包含动画区间空间切换、任意数量脊椎、Blender、Face、UI 或外部工具扩展。
