# 核心身体控制与蒙皮贯通

本里程碑分三组交付，完成一组后继续下一组；部分通过不代表整个 goal 完成。

| 组 | 状态 | 交付与证据 |
| --- | --- | --- |
| 脊柱 IK/FK | 已实现并验证 | 固定 Root / Spine1 / Chest 双段机制、独立基部 FK、混合、腰部 roll、双向当前姿态匹配；30 / 70 关节 Maya 后台通过 |
| 头部与手脚控制空间 | 已实现并验证 | 5 组 / 9 个约束；头部朝向、手脚 goal 与 pole 成组切换；30 / 70 关节当前世界姿态保持，支持 Undo / Redo |
| 控制到蒙皮贯通 | 待实现 | 自生成网格与显式权重贯通 Fit、Body、全身控制、变形和整体使用示例 |

## 本批次证据

- 292 项纯 Python 回归通过。
- `maya_body_spine_smoke.py` 在 70 / 30 关节场景通过：直立及弯曲姿态双向匹配、0.5 混合位置插值、骨盆与腿部隔离、颈头 / 肩胛 / 手臂跟随、全局平移 / 旋转 / 1.5 倍缩放下匹配、锁定输入拒绝、注入匹配失败回滚、匹配和构建 Undo / Redo。
- FK→IK 全 Body 世界矩阵最大元素误差约 `1.0486e-5`；IK→FK 约 `5.33e-15`；全局变换下约 `2.8024e-6`。运行时验收阈值为 `1e-4`。
- `maya_body_control_spaces_smoke.py` 在 30 / 70 关节通过：身体与 Global 跟随差异、全部空间双向切换、整体构建 Undo / Redo、锁定拒绝、注入切换失败回滚、选择与时间保持。最大矩阵误差分别约 `1.60e-14` / `1.78e-14`。
- 原 Torso-only 30 关节 smoke 通过，旧 FK 调用保持兼容。
- 本机详细结果位于忽略的 `validation/results/maya2024-body-spine*.json`，不提交运行日志。

## 当前边界与下一步

脊柱使用固定长度双段 RP IK。完全伸直匹配时，IK 目标沿伸展方向外移总长度的 `1e-5`，让 Maya 求解器落在最大伸展位置；Body 姿态仍以同一阈值读回复检，不放宽标准。完全折叠导致起止点重合时，无法确定 bend plane，当前拒绝匹配。

匹配只作用当前姿态，不写关键帧；目标已有动画输入、锁定、引用、动画层、非等比 / 反射缩放和部分混合值会被拒绝。标准场景角度单位为 degree。尚不支持脊柱拉伸、任意脊椎数量或旧角色自动改造。

空间切换重建本工具拥有的单源约束，保留当前帧 offset 的世界姿态；约束上的动画 / 外部输入、外部消费、目标锁定等在写入前拒绝。该操作不写动画键，也不保持其他帧的历史运动。

下一批复用显式蒙皮和权重合同，贯通完整角色与自生成网格的代表性动作及变形。先成组实现，再集中验收，不增加外围功能或独立发布版本。
