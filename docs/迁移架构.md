# 迁移架构

## 结论

可行，但不应把 8 万多行 MEL 一次性机器翻译成一个同样巨大的 Python 文件。目标是保持行为的“绞杀式迁移”：原 MEL 在一段时间内仍是基准实现，Python 每次接管一个边界清楚、可对比的能力。

## 为什么不像编译产物

当前 `AdvancedSkeleton.mel` 具有可读变量名、功能注释、版本兼容分支、直接构建 Maya UI 的命令、历史注释代码和大量独立 `global proc`。这些特征更符合长期手工维护并发布为单文件的 MEL 工具。即使作者内部可能使用文本拼接或发布脚本，核心文件里也没有足以证明“由结构化工程编译生成”的映射、模块标记或稳定生成边界。

套件层面确实存在发布期代码注入的迹象：`panel.py` 带有明确的 `EMBED START/END` 标记，并会读取自身源码生成嵌入式脚本；`panel.mel`、`picker.mel`、两个 Selector 脚本与主文件各共享 298 个过程名。因此更准确的判断是：作者很可能使用了复制公共库或生成发布载荷的流程，但没有证据表明核心 8.5 万行 MEL 是从一套结构化 Python 源工程编译而来。

## 目标边界

```text
adv_py
├── application     # 用例编排、事务、进度、错误语义
├── domain          # FitSkeleton、构建选项、命名与验证规则
├── features
│   ├── body        # 身体 Fit / Build / ReBuild
│   ├── face        # 面部拟合、Build Step、表情与口型
│   ├── deform      # Skin、BlendShape、DeltaMush、Wrap
│   ├── mocap       # 动捕映射与连接
│   └── export      # FBX、Unreal、MetaHuman
├── maya            # maya.cmds / maya.api.OpenMaya 适配层
├── ui              # PySide 界面，只调用 application 用例
└── legacy          # 未迁移 MEL 的显式兼容边界
```

依赖方向固定为 `ui -> application -> domain`，Maya 与 Legacy 作为外围适配器注入。业务模块不直接读取 UI 控件，也不把全局 MEL 变量继续伪装成 Python 模块全局变量。

## 推荐迁移顺序

1. 冻结输入版本与哈希，生成过程清单和调用关系。
2. 先迁移无 UI、无 `eval`、无全局写入的叶子工具过程，例如命名、标签、数组与数学辅助逻辑。
3. 建立 Maya Scene Adapter，把节点查询与修改集中起来，并用 Undo Chunk 包住单个用例。
4. 迁移 FitSkeleton 的读取、校验和单个编辑动作。
5. 迁移 Body Build/ReBuild 的阶段编排；每一阶段保留旧 MEL 对照入口。
6. 再处理 Deform、Export、MoCap；Face 最后迁移，因为它规模大、动态命令与 UI 状态耦合最重。
7. 所有功能稳定后再替换主 UI，最后移除 Legacy Bridge。

## 两阶段开发边界

### 第一阶段：Maya Python 重构

主线只处理 Maya：从 MEL 清单与调用关系中选择边界清楚的能力，先建立 Python 领域模型与 Maya Scene Adapter，再用本机授权 ADV 行为做结果对照。每个切片必须能在 Maya 中独立运行、复检和回滚；未迁移部分继续通过明确的 Legacy Bridge 调用，不做全量机械翻译。

第一阶段的完成门槛不是“过程数量翻译完”，而是核心身体流程已经形成稳定模块：Fit 数据读取与校验、骨架生成、IK/FK、控制器、重建、基础变形及保存/加载合同都有 Maya 侧可运行用例；核心逻辑不依赖 UI 状态或 MEL 全局变量；主流程不再需要大范围回退到单体 MEL。

### 第二阶段：迁移到 Blender

只有通过第一阶段门槛的功能才进入 Blender。迁移单位是已经验证的 Python 语义合同，而不是 MEL 过程；Blender 适配器负责 Armature、约束、驱动、坐标系和数据持久化差异。Maya 仍作为行为基准，但第二阶段不改变已经稳定的核心语义来迁就某个 Blender API。

当前两节骨架和三关节 IK/FK Blender 实现仅用于证明架构方向可行，暂时冻结功能范围。除修复防回归问题外，不在第一阶段继续增加 Blender 控制器、蒙皮、UI 或高级 limb 能力。

## Maya 切片 01：FitSkeleton 关节标签

首个 Maya-only 切片把标签文本、用例编排和 Maya 属性写入拆成三层。调用方显式传入关节列表与标签，不再读取当前选择或 UI 控件；application 层先解析全部关节，任何缺失、重名或非 joint 输入都会在修改前失败；Maya 适配器在一个 Undo Chunk 内写入 `drawLabel`、`type` 与自定义标签，并提供读取和隐藏操作。

该切片保留 Maya 标准 label type 语义，未知业务标签统一使用 Maya 的 Other 类型保存原文本。验证使用自行创建的关节，不读取 ADV 场景或资产。下一批 FitSkeleton 工作继续沿用同一边界，优先处理 Fit joint 元数据的读取与校验，不扩展 Blender。

## Maya 切片 02：Fit joint 元数据审计

第二个 Maya-only 切片把 `MayaJointLabelHost` 收拢为统一的 `MayaFitJointHost`。适配器负责把 Maya 动态属性转换为 `FitJointMetadata`，application 层只接收显式关节列表，核心审计不导入 Maya。

当前数据合同覆盖 Twist/Bendy、Inbetween/UnTwister、镜像开关、`childOfPart`、global 行为、世界朝向和 IK Local 模式。审计会报告 Twist 与 Inbetween 并存、依赖项缺失、负数量及范围越界。该用例完全只读；Maya 2024 验证确认读取和失败预检不会把场景标记为已修改。

## Maya 切片 03：声明式元数据变更

`FitJointPatch` 用字段和值描述期望状态，其中 `None` 明确表示删除属性。`EditFitJointMetadata.plan()` 会读取全部目标关节、预测变更后的完整状态、运行同一套核心审计，并返回逐关节、逐字段的前后差异；预览阶段不修改 Maya 场景。

`apply()` 只在计划非空时打开一个 Maya Undo Chunk。Maya 适配器按声明创建、更新或删除动态属性，application 随后重新读取所有目标字段；属性存在性或值不一致、以及最终审计失败都会在事务内部抛错并撤销本次修改。Maya 2024 自生成场景验证覆盖 Twist/Bendy 到 Inbetween 的模式切换、枚举属性、非法组合拦截、复检和一次 Undo 恢复。

## Maya 切片 04：层级快照与构建前校验

`MayaFitJointHost.capture_fit_hierarchy()` 解析唯一的 FitSkeleton transform，一次采集全部后代 joint 的完整 DAG 路径、直接父节点、本地位置和世界位置，并按父级优先稳定排序。Maya API 只存在于适配器；`InspectFitHierarchy` 把不可变快照交给纯 Python 核心审计。

当前构建前条件覆盖：恰好一个直接根关节、根名为 `Root`、根相对容器位于中心容差内、每个后代的直接父级也是快照内 joint、短名唯一、名称不以下划线分隔且不以数字开头，以及合成快照中的父链循环。命名空间只属于宿主路径，不参与业务短名比较。审计全程只读，既可返回完整问题列表，也可由调用方在正式构建前统一拒绝。

本切片刻意没有推断 `_L/_R` 等侧向后缀。现有证据只足以确认 Fit joint 的通用名称限制和中心判断；世界位置已保留在快照中，待镜像配对语义被独立验证后再增加规则，避免把其他 ADV 对象的命名习惯错误固化为 FitSkeleton 合同。

## Maya 切片 05：容器设置补齐

`FitSkeletonSettings` 用有序字段和值表达容器上的显示开关、构建选项、对象记录和 ReBuild 脚本文本。核心层负责类型、显示几何枚举和 `visGap` 范围；Maya 适配器负责属性名、Maya 数据类型及 keyable 状态，两者之间不共享 UI 控件或 MEL 全局变量。

`EnsureFitSkeletonSettings.plan()` 读取现有容器并列出缺失字段。已有字段类型错误或值非法时，预检直接失败；`apply()` 只在确有缺失字段时打开一个 Undo Chunk，逐项补齐后重新读取完整快照，同时确认已有值没有被改写。失败会撤销本次补齐。ReBuild 脚本在这个用例里只作为不透明字符串保存和复检，不会求值或执行。

显示间距默认值作为显式参数传入，默认 `0.75`，不读取 `optionVar` 或界面状态。这样既保留已确认的默认行为，也让未来配置来源由 application 调用方决定。Maya 2024 自生成场景验证覆盖只读预览、18 个缺失字段的增量补齐、已有值保留、脚本文本不执行、非法属性类型拦截和一次 Undo 恢复。

## Maya 切片 06：非破坏性容器创建

`FitContainerSpec` 描述根级短名、显示半径、Y/Z Up、环形语义和 Fit 显示样式。`CreateFitSkeleton.plan()` 从 Maya 适配器读取当前场景 Up Axis，并扫描整个场景中的同名节点；计划阶段不读取选择、不修改场景。任何类型的同名节点都会阻止执行，既不删除，也不依赖 Maya 自动改名。

执行阶段在一个 Undo Chunk 内创建根级圆环、应用宿主显示样式、锁定平移与旋转，并复用第五切片的设置合同补齐 21 项默认值。创建后重新读取容器状态和设置，检查根级路径、名称、形状、显示样式、零位姿、锁定通道、圆环平面与半径，以及设置所属节点和所有默认值；任一检查失败都会撤销整个创建事务。

Maya 适配器在创建曲线前保存当前选择并在命令结束后恢复。Maya 2024 自生成 Z-Up 场景验证确认计划只读、选择不变、同名二次创建被阻止、已有容器不受影响、一次 Undo 删除本次创建且无关节点继续存在。

## Maya 切片 07：自生成 Root/Spine 基础模板

本机清单显示完整身体 FitSkeleton 依赖外部模板导入，没有适合直接迁移的独立模板构建过程。本切片不解析模板文件，改用独立设计的 `FitTemplateSpec` 与三关节基线 `Root → Spine1 → Spine2`，为 Python 侧提供可运行的 Fit 编辑入口和合法测试素材。模板根据 Maya 当前 Y/Z Up 生成局部偏移，段长由调用方显式提供。

核心层检查唯一 `Root`、父级存在性、重复名称、循环、有限位置并生成稳定的父级优先顺序；创建后的不可变层级快照会再次与模板逐关节比较父级和局部位置。application 层要求目标容器已具备完整设置且尚无任何关节，并扫描整个场景中的同名节点。任一阻塞都在事务前返回，既不合并旧层级，也不自动改名。

执行阶段在一个 Undo Chunk 内逐级创建 joint 并写入 Maya 标准 Root/Spine 标签，随后复检层级、标签和容器设置未被改写。Maya 2024 自生成 Z-Up 场景验证确认计划只读、选择保持、重复创建被阻止、一次 Undo 只移除三个新关节，容器、设置和无关节点继续存在。

## Maya 切片 08：声明式 Fit joint 位置编辑

`FitJointPositionPatch` 以显式关节路径或唯一短名描述期望的本地位置。纯 Python 计划先审计完整 FitSkeleton 层级，再解析全部目标、过滤没有实际变化的轴，并检查这些轴在快照中是否可写；锁定或被连接驱动的变化轴会在事务前失败。Root 的本地 X 仍受中心容差约束。

Maya 层级快照现在额外记录每个 joint 的锁定轴和可写轴。适配器只对计划中的变化轴分别调用 `setAttr`，因此其他锁定轴不会造成无关阻塞，也不会被自动解锁或断开连接。批量写入位于一个 Undo Chunk；执行后重新采集层级和容器设置，确认节点集合、父级、短名、锁定/可写状态、未编辑的本地位置及所有目标值一致。

移动父关节会按 Maya DAG 正常带动后代世界位置，但后代本地位置保持不变。本切片不调用耦合 UI、临时节点与多种朝向分支的旧更新过程，也不自动重算 `jointOrient` 或刷新可视化几何。Maya 2024 自生成场景验证覆盖 Root 离中拦截、锁定轴拦截、双关节批量修改、选择保持、层级保持和一次 Undo 恢复。

## Maya 切片 09：简单单子链朝向

`FitOrientationSnapshot` 把层级、场景 Up Axis、jointOrient、rotate、世界轴和 jointOrient 可写状态组合成不可变输入。`FitOrientationRequest` 只接受显式目标；纯 Python 计划要求每个目标恰好有一个直接 joint 子级、rotate 为零、目标 jointOrient 可写，并要求全部后代 translate 可用于位置补偿。

朝向语义固定为本地 X 指向直接子级，本地 Y 尽量贴近场景正 Up。若 Aim 与 Up 近似平行，计划从其他世界正轴中确定性选择正交参考，并记录期望的世界 X/Y 轴。Maya 适配器使用显式 joint 路径执行朝向，不依赖选择；执行后恢复 Maya 隐式改写的直接子级 jointOrient，并按父到子顺序恢复全部后代世界位置。

application 在同一个 Undo Chunk 内按层级顺序应用多个目标，随后复检世界轴、零 rotate、所有 joint 世界位置、末端 jointOrient、层级结构、锁定/可写状态和容器设置。已达到期望轴向的目标不会重复写入，因此再次计划为空操作。

首次真实验证发现 Maya 会隐式改写直接子 joint 的 jointOrient；加入恢复后，第二次验证进一步发现后代世界位置需要显式补偿。两次失败都在事务内自动撤销。最终 Maya 2024 自生成 Z-Up 验证通过锁定预检、双关节朝向、末端保护、位置保持、选择保持、幂等计划和一次 Undo 恢复。

当前只支持无分支单子链和固定 X-Aim/Y-Secondary，不覆盖多子级选择、自由朝向、worldOrient 写入或镜像 limb。

## Maya 切片 10：worldOrient 朝向策略预检

`parse_world_orientation_policy` 将 `worldOrientUp` 的 Up/Down 与 `worldOrientForward` 的 Forward/Backward 解析为带正负号的本地轴方向；`free` 被保留为明确的自由 Forward，而不是偷偷替换为场景轴。解析会拒绝只有 Forward 没有 Up、未知枚举，以及 Up/Forward 使用同一本地轴的冲突配置。

Maya 朝向快照现在同时采集每个 joint 的可选元数据。简单单子链计划会在处理目标前解析 worldOrient；检测到该策略时，明确说明已识别的本地轴组合，并在事务开始前拒绝交给 X-Aim/Y-Secondary 写入器。这个边界避免两种语义被混写，也没有复制旧实现会自动修改冲突枚举的副作用。

本切片仅建立宿主无关策略合同和安全预检。后续 Maya-only 切片将为固定 Forward 组合建立独立的世界基准朝向计划、矩阵写入和 Undo 复检；`free`、World Match、轴交换与对称分支继续保持未实现。

## Maya 切片 11：固定 worldOrient 写入

`world_axes_from_orientation_policy` 把两条带正负号的本地轴约束展开为完整右手系世界 X/Y/Z 轴。当前证据对应世界 Up `+Y`、世界 Forward `+Z`，所以计划明确要求 Maya Y-Up；Z-Up 不做推断式换轴。`plan_world_fit_orientations` 只接受同时具有固定 Up/Forward 的单子链目标，并复用 jointOrient、rotate、子 jointOrient 和全部后代 translate 的可写预检。

`OrientWorldFitJoints` 与原 X-Aim 用例分离。Maya 适配器将计划出的正交世界矩阵写到目标 joint，再把 rotate 烘入 jointOrient，恢复直接子级 jointOrient，并按父到子顺序恢复后代世界位置。整个批次位于一个 Undo Chunk，执行后复检世界轴、零 rotate、层级、位置、元数据、容器设置和非目标 jointOrient；已匹配目标轴时计划为空。

Maya 2024 自生成 Y-Up 场景验证覆盖固定 `xDown/zForward`、只读预览、选择保持、元数据保持、幂等、一次 Undo 恢复，以及 `free` 在事务前拦截。World Match、轴交换、对称修正与自由 Forward 尚未进入合同。

## Maya 切片 12：free Forward 世界朝向

朝向快照新增 `FitOrientationAxisConfiguration`，以只读方式保存 FitSkeleton 可选的 `primaryAxis`、`secondaryAxis` 和 `worldmatch`；属性不存在时使用已确认的 X/Y/关闭默认值。轴枚举先转成带正负号的本地轴，并拒绝 primary/secondary 同轴配置。它们没有被加入容器的必备设置，因此不会为了读取朝向而修改旧场景。

当 `worldOrientForward=free` 时，计划将目标到直接子级的向量投影到世界 XZ 平面，再让全局 `secondaryAxis` 沿该水平参考方向，同时保持 worldOrient 指定的本地 Up 轴朝向世界 `+Y`，最后补全右手系第三轴。子级与目标在水平方向重合时无法确定 roll，计划会在事务前失败；固定 Forward 仍沿用世界 `+Z`。

Maya 写入器继续复用切片 11 的矩阵、后代位置补偿、单次 Undo 和构建后复检。Maya 2024 自生成斜向单子链验证覆盖 free 的只读计划、世界轴结果、位置保持、幂等与 Undo；同时确认 `worldmatch` 开启时不会进入当前写入器。World Match、轴交换与对称修正仍未实现。

## Maya 切片 13：显式分支子级朝向

`FitOrientationRequest.child_selections` 用 `FitOrientationChildSelection(parent, child)` 明确多子级关节的 Aim 分支。唯一子级仍可省略；存在多个直接 joint 子级时，缺少选择、父关节未包含在目标中、子级不存在或不是直接子级都会在事务前失败。计划不复制中心线、DAG 顺序或名称偏好的隐式选择规则。

`FitOrientationChange` 现在记录全部直接子级的原始 jointOrient。单子链继续使用已验证的 Maya joint 朝向命令；多分支使用计划生成的完整 X/Y/Z 世界矩阵，避免 Maya 命令自行选择第一个子级。两条路径都会恢复所有直接子级 jointOrient，并按父到子顺序恢复完整后代世界位置。

首次 Maya 2024 分支验证确认 Maya 的 joint 朝向命令不会服从外部选择的 Aim 子级，复检因此触发事务回滚。改为多分支矩阵路径后的隔离复验通过：显式 Clavicle 分支用于 Aim，兄弟 Spine 分支与 Clavicle 分支的 jointOrient、所有后代位置、选择、幂等计划和单次 Undo 均保持。分支自动推断、对称和特殊身体部位规则仍未进入合同。

## Maya 切片 14：通用模板事务与自生成上半身树

原 `CreateMinimalFitTemplate` 的创建、预检和复检逻辑提取为 `CreateFitTemplate`，调用方必须提供已经通过核心校验的 `FitTemplateSpec`。通用用例检查空容器、完整设置和全场景名称冲突，在一个 Undo Chunk 中按父级顺序创建全部 joint、写入标签，再逐项复检拓扑、局部位置、标签和容器设置。三关节入口作为薄兼容包装继续使用同一条执行路径。

`synthetic_upper_body_fit_template` 是独立设计的 14 关节 T-pose 测试树：Root、两节 Spine、Neck/Head/HeadEnd，以及左右 Collar/Shoulder/Elbow/Hand 链。Spine2 同时连接 Neck、ClavicleLeft 和 ClavicleRight，用于承载真实分支场景；Y-Up/Z-Up 只改变躯干方向，左右臂沿世界 X 展开，统一 `scale` 控制比例。模板完全由仓库代码生成，不读取、转换或提交授权安装中的身体模板。

Maya 2024 自生成 Z-Up 验证先复演三关节兼容入口，再在同一隔离进程中创建 14 关节上半身树，确认 Spine2 三分支、全部标签、只读预览、重复创建拦截、选择保持和单次 Undo。当前模板用于推进身体核心架构，不等同于完整人物 FitSkeleton；骨盆、腿、脚、手指、镜像和专用元数据尚未包含。

## Maya 切片 15：上半身创建与朝向单事务编排

`BuildSyntheticUpperBodyFit` 只负责编排已有的通用模板与显式分支朝向用例。它要求目标 FitSkeleton 位于根级、平移/旋转归零并锁定、缩放为一、设置完整且内部为空；这些条件与场景级重名检查都在任何写入前完成。

计划阶段用模板局部坐标预测完整绝对 DAG 路径和世界位置，再生成 11 个非末端关节的 X-Aim/Y-Secondary 变更。Spine2 的三个直接子级不会依赖 DAG 顺序或名称猜测，而是明确选择 Neck；直接子级列表在核心层排序，使预测与不同宿主返回顺序无关。

模板创建与朝向都提供受控的“事务内执行”入口，公开的独立用例仍各自保持原有单次 Undo 行为。组合用例只打开一个外层 Undo Chunk；关节创建完成后会重新采集真实场景并生成实际朝向计划，只有它与零修改预演完全一致才继续写入。创建复检、计划一致性或朝向复检任一失败，都会撤销本次创建的全部 14 个关节。

Maya 2024 独立 Z-Up 进程验证确认：预演不污染场景，14 个关节和 11 个朝向一次提交，Spine2 使用 Neck 分支，世界位置、零 rotate、选择与容器设置保持；重复构建在事务前被阻止，一次 Undo 删除整棵上半身树而保留容器和无关节点。本切片仍不加入骨盆、腿、脚、手指、镜像、World Match 或 Blender 实现。

## Maya 切片 16：单侧源腿与全身声明式编排

本机 MEL 清单中没有边界独立的腿部创建过程；相关更新集中在大型 Fit joint 更新器，完整身体则由外部 biped 模板导入。直接翻译该更新器会重新引入单体耦合。本切片只用本机已授权模板确认可观察的 `Root → Hip → Knee → Ankle`、Ankle 的 Heel/Toes 分支，以及 Toes 的足侧/末端分支；仓库不保存该模板、属性、数值或源码片段。

本切片最初在已有 14 关节上半身树上增加 8 个独立设计的源腿关节，用来验证 Root、Ankle 和 Toes 的多分支创建/朝向。位置由代码根据 Y/Z Up 和显式 scale 生成，不采用授权模板坐标。该临时 22 关节表示随后在镜像分析切片中被收敛为真正的单侧身体源树。

第十五切片的编排逻辑下沉为 `BuildOrientedFitTemplate`。任意已验证模板和 `FitOrientationRequest` 都可复用同一套容器预检、零修改预测、创建后计划复算、单事务提交与构建后复检；`BuildSyntheticUpperBodyFit` 继续作为兼容入口。新的 `BuildSyntheticBodySourceFit` 显式指定 Root→Spine1、Spine2→Neck、Ankle→Toes 和 Toes→ToesEnd，避免三处多分支依赖 DAG 顺序。

Maya 2024 独立 Z-Up 验证确认该临时表示的源腿/足部拓扑、标签、世界位置、零 rotate、选择和容器设置。重复构建在事务前停止，一次 Undo 删除整棵树但保留容器与无关节点。首次验证的业务复检已通过，结果汇总脚本因浮点完全相等误判；改为与 application 相同的 `1e-5` 容差后隔离复验通过。

## Maya 切片 17：构建期 M/R/L 对称拓扑展开

授权行为的可观察规则表明，FitSkeleton 保存中心链和单侧源分支，而不是保存两套对称 Fit joints。沿父链遇到的第一个偏离 X 中心面的关节决定整条分支的侧向：负 X 为 Right、正 X 为 Left；可镜像分支必须从 Right 开始。`noMirror` 使该分支只产生一侧，配合 `noMirrorLeft` 时改为只保留 Left。两项元数据会沿后代继承。

据此，`synthetic_body_source_fit_template` 收敛为 18 个关节：Root/Spine1/Chest/Neck/Head/HeadEnd 六个中心关节，Scapula/Shoulder/Elbow/Wrist 四个负 X Right 臂关节，以及 Hip/Knee/Ankle/Heel/Toes/FootSideInner/FootSideOuter/ToesEnd 八个负 X Right 腿足关节。此前用于朝向压力测试的 14 关节显式双臂模板仍作为独立测试入口，不参与构建期镜像。

核心 `expand_fit_symmetry` 在不导入 Maya 的情况下生成带 `_M/_R/_L` 后缀的逻辑输出路径、父子关系和世界位置。中心节点只产生 M；默认 Right 源分支产生 R 与 YZ 平面反射后的 L；`noMirror`/`noMirrorLeft` 规则按祖先传播。元数据不完整、配置冲突、非法中心容差或未标记的 Left 起始分支都会在产生构建计划前失败。`PlanFitSymmetry` 只负责从 Maya 适配器读取完整快照与设置，不修改场景。

Maya 2024 独立 Z-Up 验证确认 18 个真实 Fit 源关节只读展开为 30 个逻辑实例：6 M、12 R、12 L。输出父链、YZ 位置反射、Right 起始位置、选择保持和 Left-only 继承均通过；元数据一次 Undo 恢复默认展开，源树一次 Undo 完整移除，容器和无关节点保留。本切片不创建镜像 Fit joints，也不宣称已完成 Maya `mirrorBehavior` 朝向；下一切片将以此计划为输入开始物化构建骨架。

## Maya 切片 18：基础 Body skeleton 物化

本机清单中的完整骨架创建过程仍有 573 行，并与后续 Body rig 阶段相连。本切片没有迁移该过程，而是把上一个切片已经稳定的对称实例定义为唯一输入，只实现最早的 joint DAG 物化边界。`BodyJointSpec` 保存源 Fit joint、目标完整路径、父路径、M/R/L side、世界位置和标签；`BodySkeletonSnapshot` 保存 Maya 实际读取的相同状态以及 rotate/jointOrient。

`BuildBodySkeleton.plan()` 先执行只读对称展开，再读取全部源标签并扫描场景中 30 个目标短名。任一标签缺失或任一类型同名节点存在时都不会打开事务。执行时按父级优先顺序调用独立的 Body host 端口；创建后的纯 Python 审计逐关节核对集合、父子路径、side、标签、位置和中性朝向，并重新读取 Fit 朝向快照与容器设置，确保构建没有改写输入。

`MayaBodyBuildHost` 继承已验证的 Fit 读取与 Undo 基础设施，只实现 Body joint 创建和快照采集。它使用 `createNode(..., skipSelect=True)` 保持用户选择，显式写入 Maya joint side，并在父级下按世界位置放置每个关节。当前不调用旧骨架创建过程，不生成控制器、约束、IK、变形骨或显示层。

Maya 2024 独立 Z-Up 验证确认：18 个 Fit 源关节在场景零修改预演后生成 30 个 Body joints（6 M、12 R、12 L）；根、臂和腿的父链、世界位置、标签、side 与零 rotate/jointOrient 全部通过，Fit 输入和选择保持。二次构建因名称冲突在事务前停止，一次 Undo 只删除 30 个 Body joints，FitSkeleton、18 个源关节和无关节点继续存在。下一切片将单独研究并写入 Right/Left 镜像行为朝向。

## Maya 切片 19：Body 镜像行为朝向

对 YZ 位置反射直接复用完整旋转矩阵会产生左手坐标系，不能作为 Maya joint 的稳定行为朝向。本切片把约定收敛为宿主无关算法：M/R 输出沿用 Fit 源的 X Aim、Y Secondary 与 Z；L 输出分别反射 X Aim 和 Y Secondary 的世界 X 分量，再用 `X × Y` 重建并归一化 Z。输入帧必须是有限、正交的右手系，非法帧会在场景修改前失败。

`OrientBodySkeleton.plan()` 同时读取 Fit 朝向/设置和现有 Body 快照，逐关节核对集合、名称、父级、side、世界位置、零 rotate 与完整 `jointOrient` 可写性。执行阶段只打开一个 Undo Chunk，按父级顺序写入世界轴，再恢复 30 个关节原有世界位置；事务内复检结构、标签、位置、目标世界轴和 Fit 输入，任何漂移都会撤销全部朝向修改。已经满足目标的骨架返回零修改计划，不制造空事务。

Maya 2024 独立 Z-Up 验证确认：30 个 Body joints 全部得到源或镜像行为朝向，12 个 Left 帧均保持右手系；世界位置、零 rotate、Fit 源、容器设置和用户选择保持不变。重复规划为零修改，一次 Undo 恢复完整的中性 Body skeleton 而不删除骨架、FitSkeleton 或无关节点。本切片仍不加入控制器、IK/FK、蒙皮、World Match 或 Blender 实现。

## Maya 切片 20：Body 创建与朝向原子编排

前两个切片分别提供可独立调用的 `BuildBodySkeleton` 和 `OrientBodySkeleton`，适合检查中间状态，但完整构建需要两个 Undo Chunk。如果第二步因宿主状态变化失败，中性 Body 会继续留在场景中。`BuildOrientedBodySkeleton` 因此成为新的黄金路径：事务前只执行一次完整 Fit/设置/标签/名称预检，事务内先物化并审计中性骨架，再从实际快照计算朝向变化，写入世界轴、恢复位置并完成最终复检。

原有分步用例仍保留。它们与原子用例共享 `_materialize_body_skeleton` 和 `_apply_body_orientation` 两个 application 内部步骤，避免维护两套创建或朝向语义；顶层用例单独拥有事务，Maya adapter 不需要感知编排差异。任何创建路径漂移、中性骨架审计失败、朝向不可写、最终结构/位置/世界轴漂移或 Fit 输入变化，都会从同一个异常路径撤销整棵新建 Body。

Maya 2024 独立 Z-Up 验证确认：零修改预演后，一个 Undo Chunk 创建并朝向 30 个 Body joints；30 组世界轴、世界位置、Left 右手帧、Fit 源和用户选择全部通过复检。重复构建在事务前因名称冲突停止，一次 Undo 删除整棵已朝向 Body，同时保留 FitSkeleton、18 个 Fit 源关节和无关节点。本切片不扩展 Blender，也不加入控制器、IK/FK、蒙皮或 ReBuild。

## Maya 切片 21：版本化 Body 所有权审计

ReBuild 的删除阶段不能只凭 `Root_M` 或 `_M/_R/_L` 命名判断归属；这些名称可能来自用户、原 ADV 或其他绑定工具。本切片先定义独立的 `BodySkeletonProvenance`：owner、artifact kind、schema version、Fit 来源完整路径和预期 Body joint 数量。该合同只描述本 Python 工程自己的产物，不复用或推断原 ADV 的私有属性。

`BuildOrientedBodySkeleton` 在创建和朝向完成后、同一 Undo Chunk 关闭前写入 provenance，并重新采集 Body 快照做字段级审计。Maya adapter 只在本次新建的根 joint 上创建五个 `advPy*` 动态属性，写入后全部锁定；任一属性预先存在、写入失败或复检不符都会撤销整棵 Body。`InspectBodySkeletonProvenance` 是纯读取用例，调用方显式提供预期 Fit 来源和 joint 数量，返回稳定 issue code，不自动删除、解锁或修复场景。

Maya 2024 独立 Z-Up 验证确认：原子构建得到五个值正确且锁定的 provenance 属性；正确合同审计为 owned，错误 joint 数量返回 `provenance_body_joint_count_mismatch`，两次审计均不修改场景。一次 Undo 删除带标记 Body，同时保留 FitSkeleton、18 个 Fit 源关节和无关节点。后续 ReBuild 只能在所有权、结构和外部依赖都通过预检后实现替换；本切片不执行删除。

## Maya 切片 22：ReBuild DAG 与外部依赖安全评估

仅有 provenance 不能证明整棵 DAG 可以删除：用户可能在 Body joint 下挂接附件，或通过 skinCluster、constraint、animCurve 和普通 DG connection 使用关节。本切片新增 `BodyRebuildSceneState`，只保存根路径、根下全部 DAG 完整路径和结构化外部连接；连接按 animation、constraint、skin cluster、generic connection 分类，不把 Maya 查询细节放进核心判断。

`InspectBodyRebuildSafety` 读取一次当前 Fit 对称计划、Body 快照和 Maya 依赖快照，再由纯 Python `audit_body_rebuild_safety` 检查 provenance、预期 joint 集合、名称/父级/side、完整 DAG 集合及外部连接。任一缺失或计划外 joint、父链漂移、非 joint attachment、shape 或外部 DG consumer 都会生成稳定 issue code；`safe_to_replace` 只有在 issue 为空时成立。该结果是后续替换事务的必要前置条件，不会自行删除、断开连接或修改场景。

Maya 2024 独立 Z-Up 验证确认：未附加内容的 30 关节 Python Body 通过安全评估且读取不改变 scene modified 状态；临时挂在 Chest 下的 transform 触发 `unexpected_dag_descendant`，Root message 到外部 network 的连接触发 `external_connection`。移除两项合成依赖后重新通过评估，一次 Undo 删除 Body 并保留 FitSkeleton、18 个 Fit 源关节和无关节点。本切片仍不执行 ReBuild 删除。

## Maya 切片 23：受保护的原子 Body ReBuild

`ReplaceOwnedBodySkeleton.plan()` 先执行完整 ReBuild 安全评估，再生成新的原子 Body 构建计划。旧 Body 的实际 30 个完整路径可以从名称冲突中排除；场景其他位置的任何同名节点仍保留为 blocker。两次读取的 Fit 朝向、设置和展开计划必须完全相同，否则预检报告 source drift，不打开替换事务。

执行阶段在删除前再次读取旧 Body、DAG/连接、Fit 朝向和容器设置，并与计划快照逐项比较，关闭预检与写入之间的竞态窗口。确认未漂移后，Maya adapter 只删除已经通过归属审计的精确根路径；application 在同一 Undo Chunk 内复用完整物化、朝向、位置恢复和 provenance 写入步骤，并再次运行 ReBuild 安全评估。创建、朝向、标记或最终审计任一失败，事务回滚会同时移除新树并恢复旧树。

Maya 2024 独立 Z-Up 验证确认：修改合成 Fit Hip 后，ReBuild 预演只忽略旧 Body 自身的 30 个同名路径；执行后 Root UUID 改变，Hip_R 世界位置跟随新 Fit，30 关节新树重新通过安全评估，Fit 输入和选择保持。一次 Undo 恢复旧 Root UUID 与旧 Hip_R 位置，同时保留已经编辑的 Fit。当前替换仍拒绝含 attachment、shape、skinCluster、constraint、animation 或其他外部连接的 Body，不会擅自保存或修改引用文件。

## Maya 切片 24：双侧手臂 FK 控制首个闭环

`BodyArmFkControlPlan` 是独立于 Maya 的控制规格，只从已经通过安全评估的 Body 快照提取 Shoulder、Elbow、Wrist 的路径、父链和世界轴。左右两侧各生成三级 offset/control 层级，控制器初始世界帧与对应 Body joint 对齐，控制通道保持归零；核心审计核对节点集合、层级、曲线类型、世界帧、约束名称和 source/driven 连线，不包含 `maya.cmds`。

`BuildBodyArmFkControls` 在写入前复用 Body provenance、DAG 和外部连接安全评估，并扫描控制根、offset、control 与 constraint 的全部名称。Maya adapter 只在一个 Undo Chunk 内创建根 transform、6 个 NURBS curve 控制和 6 个 orientConstraint；每次曲线及约束创建都恢复用户选择。构建后重新采集控制系统并确认 Body、Fit 朝向及容器设置没有变化，任何漂移都会撤销整套新节点。

Maya 2024 独立 Z-Up 验证确认：只读预演不修改场景，六个控制器均为零本地位姿且连线正确；右肩控制能改变真实 Body 手臂姿态，归零后腕部回到绑定位置。控制存在时 ReBuild 因外部 constraint 明确拒绝；一次 Undo 完整移除控制系统后 Body 再次满足 ReBuild 安全条件，Fit 与无关节点继续存在。本切片不加入 IK、FK/IK 切换、腿、手指、控制器形状库、UI 或 Blender 实现。

## Maya 切片 25：双侧 Arm FK/IK 机制关节链

完整 IK/FK 不能让同一套 Body joints 同时承担 FK 操作、IK 求解与最终变形结果。本切片新增 `BodyArmMechanismPlan`，从当前 Body 快照为左右两侧各声明 FK 与 IK 两条 Shoulder→Elbow→Wrist 驱动链。每个规格保存 role、side、Body 来源、完整父路径、世界位置和世界轴；纯 Python 审计核对 12 个关节的集合、父链、来源、side、世界帧和零 rotate。

`BuildBodyArmMechanisms` 在场景修改前复用 Body 所有权/DAG/外部连接安全评估并扫描全部目标名称。Maya adapter 在一个 Undo Chunk 内创建根 transform 和四条机制链，把旋转冻结到 jointOrient，并通过独立 `advPySourceJoint` message 属性连接对应 Body joint。构建后重新采集机制链，同时确认 Body 快照、Fit 朝向和容器设置没有变化；任一创建或复检失败都会撤销整套机制结构。

Maya 2024 独立 Z-Up 验证确认：12 个机制 joint 的来源、父链、side、世界帧与零 rotate 全部通过；旋转右侧 FK Shoulder 会带动其 FK Wrist，但右侧 IK Wrist 和 Body Wrist 保持不变。来源 message 存在时 ReBuild 以外部连接拒绝替换；一次 Undo 移除全部机制节点和连线后，Body 再次满足 ReBuild 安全条件。本切片尚未创建 IK Handle、Pole Vector、FK 控制到机制链的连接或机制链到 Body 的双源混合，也不扩展 Blender。

## Maya 切片 26：FK 控制接入 FK 机制链

上一切片的直接 FK 用例仍作为最小 Body 控制基线保留；完整 Arm rig 改用新的 `BuildBodyArmFkMechanismControls`。该用例通过当前 Fit 对称计划计算预期 Body provenance，读取实际 Body，并用 `BodyArmMechanismPlan` 对 12 个已存在机制 joints 做来源、父链、side、世界帧和零 rotate 审计。只有机制链完整时，核心 FK 控制规划才把每个 Body source path 显式映射到对应 FK driver path；同一控制规格因此无需包含 Maya 类型。

执行阶段沿用 6 个 NURBS control、offset 层级和 orientConstraint 的已验证 Maya 端口，但约束目标从 Body joints 改为 FK driver joints。所有节点在一个 Undo Chunk 内创建，构建后再次核对控制连线、Body、机制链、Fit 朝向和容器设置；损坏的来源 message、机制姿态或同名节点在写入前拒绝，运行期复检失败则只回滚控制层。

Maya 2024 独立 Z-Up 验证确认：控制约束的六个 target 全部是 FK driver，而不是 Body joint；旋转右 Shoulder FK control 会移动 FK Wrist driver，IK Wrist driver 与 Body Wrist 保持不变。第一次 Undo 只移除控制层并保留 12 个机制 joints，第二次 Undo 再移除机制层及来源 message，Body 随后恢复 ReBuild 安全状态。本切片尚未创建 IK controls、IK Handle、Pole Vector、IK/FK blend 属性或机制链到 Body 的双源约束，也不扩展 Blender。

## Maya 切片 27：双侧 Wrist/Pole Vector RP IK

`BodyArmIkPlan` 从 Body 手臂三点和 IK mechanism paths 生成左右两侧的 Wrist control、Pole Vector control、RP IK Handle 与约束规格。Pole Vector 使用肘部到肩腕直线投影的垂向计算，并按上下臂总长缩放；近似直臂时使用肘关节 Z 轴作为稳定回退。尺寸和距离参数必须为正有限值。

`BuildBodyArmIkControls` 在写入前核对 Body provenance 和 12 关节 mechanism 快照，扫描所有 offset、control、handle 和 constraint 名称。Maya adapter 在一个 Undo Chunk 内创建四个 NURBS controls、两个 `ikRPsolver` Handle 和两个 poleVectorConstraint，并恢复用户选择。构建后核对控制父链、求解关节列表、PV 来源、曲线形状、零通道，以及 Body/Fit 不变；允许 Maya solver 接管 IK driver 的旋转，但机制集合、父链、来源、side 和初始位置必须保持。

Maya 2024 独立 Z-Up 验证确认：右 Wrist IK control 移到手臂可达范围内时，IK Wrist driver 到达目标，Body Wrist 保持不动；一次 Undo 只移除 IK 控制与求解节点并保留 mechanism chains，第二次 Undo 再移除机制层。本切片尚未把 FK/IK mechanism 混合到 Body，也没有 IK/FK match、stretch、twist、腿部或 Blender 实现。

## Maya 切片 28：双臂 FK/IK 输出到 Body

`BodyArmBlendPlan` 为左右侧分别声明一个 0–1 blend 属性、一个 reverse 权重节点，以及 Shoulder/Elbow/Wrist 三组 FK/IK 双源关系。核心审计核对 settings 路径、属性默认值、reverse 输入、六个约束的 source/target 顺序和两路权重来源，不包含 Maya API。

`BuildBodyArmBlend` 在写入前核对 Body provenance 与绑定态 mechanism 快照，并扫描 settings、reverse 和 constraint 名称。Maya adapter 在一个 Undo Chunk 内创建 `armIkFk_R/L`、两个 reverse 和六个 orientConstraint：FK 权重连接 reverse output，IK 权重直接连接 blend 属性。采集端把 Maya 返回的 DAG 短名 plug 规范化为完整路径，创建端在成功或异常时恢复选择。

Maya 2024 独立 Z-Up 验证确认：右侧 FK=0 时 FK control 驱动 Body，IK=1 时 Wrist IK control 使 Body Wrist 到达可达目标，0.5 时约束两路权重均为 0.5；左侧属性与姿态不受右侧操作影响。IK 层与 blend 层可分别一次 Undo，Body、Fit 和用户选择保持。本切片尚未实现 IK/FK match、控制显隐、stretch、twist、腿部或 Blender 适配。

## Maya 切片 29：完整双臂 IK/FK 单事务编排

`BuildBodyArmRig.plan()` 从一次 Body ReBuild 安全快照直接生成 mechanisms、FK controls、blend 与 RP IK 四阶段规格，并在任何写入前扫描全部 joint、offset、control、constraint、handle、reverse 和 settings 名称。执行阶段不嵌套已有用例事务，而是在一个顶层 Undo Chunk 内复用相同核心规格和 Maya 端口，逐阶段采集与审计；最后阶段失败会撤销此前全部产物。

最终 Body 复检按业务语义比较 provenance、关节集合、拓扑、side、标签、世界位置/轴和零旋转容差，避免 Maya 约束求值产生的浮点差异触发逐位误判。Maya 2024 独立 Z-Up 验证确认：零修改预演后一次提交完整 Arm Rig，FK 和 IK 均能驱动 Body；一个 Undo 同时移除四层 Arm 产物，Body/Fit 与用户选择保持，并恢复 Body ReBuild 安全状态。IK/FK match、控制显隐、stretch、twist、腿部和 Blender 仍未进入本切片。

## Maya 切片 30：双臂 IK/FK 控制显隐

`BodyArmVisibilityPlan` 只表达宿主无关的显隐意图：每侧 blend 属性直接驱动 Wrist IK 与 Pole Vector offset，已有 reverse 输出驱动该侧 Shoulder FK offset。FK 的 Elbow/Wrist 控制位于 Shoulder 控制层级下，因此无需增加节点或逐个控制连线；左右侧使用各自的 `armIkFk_R/L`，不会串扰。

`BuildBodyArmRig` 在 RP IK 创建并复检后，于同一个顶层事务内连接并采集显隐来源。核心审计只比较规格与实际 incoming plug，不依赖 Maya API；任何缺失或错误连线都会让完整 Arm Rig 回滚。Maya 2024 隐藏独立进程验证确认：默认 FK=0 时 FK 可见而 Wrist/PV 隐藏，右侧 IK=1 时显示右侧 Wrist/PV 并隐藏右侧 FK，左侧保持不变；用户选择保持，一个 Undo 仍移除完整 Arm Rig。本切片采用二值工作模式，不把中间 blend 值定义为控制曲线渐隐效果。IK/FK 无缝匹配、stretch、twist、腿部和 Blender 仍未进入本切片。

## Maya 切片 31：Wrist IK 朝向驱动

RP IK Handle 负责 Shoulder/Elbow 求解与 Wrist 位置，但不能独立表达动画师对手腕末端朝向的控制。`BodyArmIkSpec` 因此增加命名明确的 Wrist orientConstraint；`BodyArmIkState` 采集其约束名、唯一控制来源和 Wrist IK driver 目标，核心审计把三者作为 IK 完整性的组成部分。完整 Arm Rig 预检同步扫描新增约束名，避免 Maya 自动改名。

Maya adapter 在同一 IK/完整 Rig 事务中创建 `Wrist IK control → Wrist IK driver` 的无偏移朝向约束，异常时随全部 IK 产物回滚。Maya 2024 隐藏独立进程验证确认：旋转右 Wrist IK control 后，其世界朝向与 Wrist IK driver、IK=1 下的 Body Wrist 一致；选择保持且一次 Undo 仍移除完整 Arm Rig。该合同是 FK→IK 无缝匹配的必要前置，本切片尚未实现自动对齐/切换命令。

## Maya 切片 32：单侧 Arm FK→IK 无跳变匹配

`BodyArmFkToIkPlan` 从当前 Body 快照取得目标侧 Shoulder/Elbow/Wrist 的世界位置和 Wrist 世界轴，并用宿主无关的几何解算生成 Pole Vector 位置。计划同时声明 Wrist/PV controls、blend plug、必需节点和必需可写通道；预检要求当前侧严格处于 FK=0，缺节点、锁定/受驱动通道或错误模式均在事务前停止。现有 IK 与 blend 审计增加“结构复检”模式，使动画姿态下仍能检查约束图，而不错误要求控制保持绑定态零通道。

`MatchBodyArmFkToIk` 在单个 Undo 事务内再次检查场景未漂移，然后先对齐 Pole Vector 与 Wrist IK 世界矩阵，最后把目标侧 blend 设为 1。提交后复检 Shoulder/Elbow/Wrist 世界位置、Wrist 世界朝向、目标 blend、另一侧 blend 与 Fit 输入；任何跳变或串扰都会回滚控制和属性。Maya 2024 隐藏独立进程使用非绑定 FK 姿态验证：右臂切到 IK 后关键姿态保持，IK controls 正确显示，左侧不变；一次 Undo 恢复 FK 模式与原姿态，第二次 Undo 移除完整 Arm Rig。IK→FK 仍留给下一切片。

## Maya 切片 33：单侧 Arm IK→FK 无跳变匹配

`BodyArmIkToFkPlan` 从当前 Body 快照记录目标侧三关节的世界位置与完整世界轴，并把 Shoulder、Elbow、Wrist FK controls 固化为父到子的有序目标。计划只要求三个控制的 rotate 与目标 blend 可写；FK translate 不参与匹配。FK 控制审计增加动画姿态下的结构模式，继续检查父链、曲线形状和 control→FK driver 约束，但不把合法的动画 rotate 误判成绑定态错误。

`MatchBodyArmIkToFk` 预检目标侧严格处于 IK=1，并在事务内重查场景状态。Maya adapter 按父到子逐个保留控制当前世界位置、写入 Body 目标世界轴，最后把 blend 设为 0；提交后复检三关节位置与朝向、FK controls 的 translate 零值、另一侧 blend 和 Fit 输入。Maya 2024 隐藏独立进程使用移动 Wrist/PV 且旋转 Wrist 的非绑定 IK 姿态验证：切换后 Body 姿态保持、FK controls 显示，一次 Undo 恢复 IK 模式与原姿态，第二次 Undo 移除完整 Arm Rig。至此 Maya Arm 已具备双向匹配；自动打关键帧、动画区间 bake、stretch 和 twist 尚未进入合同。

## Maya 切片 34：FK/IK 位移输出到 Body

Arm stretch 需要改变 Shoulder→Elbow 与 Elbow→Wrist 的段长。此前 `BodyArmBlendPlan` 只有双源 orientConstraint，机制关节的长度变化不会传到最终 Body。现在每侧 Elbow/Wrist 规格增加一个位移约束名，Shoulder 明确保持无位移约束；核心快照与审计记录 pointConstraint 的两个 driver、Body 目标和 FK/IK 权重来源。

Maya adapter 在现有 blend 事务中创建 4 个双源 pointConstraint，复用 `reverse.outputX` 与 `armIkFk_R/L`，并和 6 个旋转约束一起完成名称预检与构建后复检。绑定态使用相同 driver 位置，因此不会改变 Body；任一位移权重错误会回滚整层 blend。Maya 2024 隐藏独立进程验证确认：FK=0 时 FK driver 的段长变化传到 Body，IK=1 时 IK driver 的段长变化传到 Body；绑定态、选择和一次 Undo 均保持。该切片只建立 stretch 的输出通道，尚未创建距离比例、no-compression 或 animator stretch 属性网络。

## Maya 切片 35：双臂 IK stretch

`BodyArmStretchPlan` 从绑定态 IK mechanism 的世界位置和本地 X 轴计算两段带符号基准 translate 与总臂长，并为每侧声明独立 start、Wrist control、测距、比例、clamp、强度混合和段长输出语义。规划阶段拒绝零长度或未沿本地 X 对齐的机制链。start transform 位于 mechanism 根下，避免用 IK 求解后的 Shoulder worldMatrix 反向驱动段长而形成 DG 环。

Maya adapter 为每侧增加 `armStretch_R/L`（0–1，默认 1），用 distanceBetween 测量 start→Wrist，multiplyDivide 除以绑定臂长，clamp 把下限固定为 1，再由 blendColors 在原长比例 1 与伸长比例间混合；最终 multiplyDivide 按同一比例驱动 Elbow/Wrist IK driver 的 translateX。节点类型、操作模式、常量、全部 incoming/outgoing plug 和默认属性值都进入构建后审计，失败随完整 Arm Rig 回滚。

Maya 2024 隐藏独立进程验证确认：目标短于绑定臂长时不压缩，目标达到 1.35 倍距离时两段同比伸长且 Wrist 到达目标；stretch=0 恢复原长，0.5 得到 1.175 倍，左右侧互不影响，一次 Undo 移除完整 Rig 并恢复 Body。当前 start/controls 尚无共同全局控制层，因此未实现全局缩放补偿；伸长姿态切回固定长度 FK 也尚未纳入无跳变匹配。

## Maya 切片 36：Arm twist helper joints

`BodyArmTwistPlan` 不改变 30 关节 Body 的所有权、数量或 provenance，而是在独立的 `AdvPy_ArmTwistJoints` 根下生成 deformation helper。默认每侧 Upper Arm 与 Lower Arm 各放置 2 个关节，可配置范围为每段 1–8 个；核心层按等分比例生成稳定名称、父级、起止 Body 关节、约束名和初始世界位置，因此规划与审计均不依赖 Maya API。

Maya adapter 为每个 helper 建立双端点 parentConstraint，权重固定为 `(1 - fraction, fraction)`，旋转插值使用 shortest 模式。这样 helper 的平移和朝向会沿 Shoulder→Elbow 或 Elbow→Wrist 分布，也会跟随上一切片产生的 stretch 段长。根、关节和约束均参加完整 Arm Rig 的零修改名称预检、事务内重查和构建后复检；任一目标、权重、父级、位置或插值模式错误都会回滚整个 Rig。

Maya 2024 隐藏独立进程 PID 4012 验证确认：双臂共创建 8 个 helper；右 Wrist 绕 X 旋转 72° 时，下臂两个 helper 的四元数角差分别为 0.418879 与 0.837758 弧度，对应三分之一和三分之二分布；手臂伸长到 1.25 倍后，全部 helper 仍位于 Body 两端点的声明插值位置。用户选择保持，一个 Undo 移除完整 Arm Rig 并恢复 Body。当前实现插值的是端点完整朝向，尚不是只提取骨骼轴向分量的 swing/twist 分解，也不包含体积保持、蒙皮权重或最终 deformation 合同。

## Maya 切片 37：显式基础 Skin Bind

`SkinBindPlan` 把一个 mesh 路径、非空且不重复的 influence joint 集合、skinCluster 名称、最大影响数、closest-distance bind method 与 interactive normalization 固化为 DCC 无关合同。`BindSkin` 先读取 mesh transform、唯一非中间 mesh shape、顶点数、有效 joint 和历史 skinCluster；缺失、重名、空网格、已有蒙皮或目标节点名称冲突均在事务前停止。当前入口刻意不依赖 Maya 选择，也不猜测应该绑定哪些模型或关节。

Maya adapter 在独立 Undo Chunk 内重新采集输入防止场景漂移，再创建 skinCluster。构建后快照复检唯一 geometry、完整 influence 集合、bind method、normalization、`maxInfluences` 与 `maintainMaxInfluences`；失败会撤销本次绑定，不移除调用方已有的网格、Rig 或 Fit。该事务与 `BuildBodyArmRig` 分开，使用户可以单独撤销蒙皮绑定。

Maya 2024 隐藏独立进程 PID 44568 使用代码生成的 72 顶点圆柱臂段验证：显式绑定到右 Lower Arm 的 2 个 twist helpers 后，Wrist 绕 X 旋转 60° 产生 0.297098 的最大顶点位移；第二次绑定预演因已有 skinCluster 被拦截。用户选择保持，一个 Undo 只移除 skinCluster、恢复绑定前点位并保留完整 Arm Rig，最后无残留节点。本切片不实现自动 influence 发现、权重编辑/模板、镜像与拷贝、复杂拓扑质量评价、已有蒙皮迁移或 Blender 适配。

## Maya 切片 38：显式稀疏顶点权重

`SkinWeightEditRequest` 把 skinCluster、mesh 和一组 `vertexIndex → [(influencePath, weight)]` 固化为 DCC 无关请求。每个顶点声明的是写入后的完整非零权重集合：顶点不能重复，influence 不能重复，权重必须有限且位于 `(0, 1]`，总和在容差内为 1。输入审计再核对 skinCluster 的唯一 geometry、顶点范围、influence 成员关系、`lockInfluenceWeights` 和最大影响数。`plan_skin_weight_changes` 对当前快照与目标快照作稳定比较，只保留真正变化的顶点。

`EditSkinWeights` 在事务前返回逐顶点 before/after 计划；零变化请求直接返回，不创建空 Undo。提交时 Maya adapter 重新读取目标权重防止场景漂移，对每个目标顶点使用完整 `transformValue` 并清零未声明 influence；因为核心已验证总和为 1，写入时不依赖 Maya 隐式归一化。构建后重新读取全部目标顶点并按 influence 路径比较；任一误差、锁定或连接变化都会回滚整个权重事务。用户选择在成功和异常路径均恢复。

Maya 2024 隐藏独立进程 PID 45892 在 72 顶点合成臂段上验证：3 个顶点分别写入 0.8/0.2、0.5/0.5、0.2/0.8 的双 influence 权重，复检与目标一致；再次应用为零变化。Wrist 绕 X 旋转 60° 时目标顶点产生 0.262185 最大位移，一个 Undo 恢复自动绑定时的原权重并保留 skinCluster，最后无残留节点。本切片不包含自动平滑、hammer、镜像/拷贝、权重文件交换、批量性能优化或 Blender 适配。

## Maya 切片 39：Skin Weight JSON 往返

`SkinWeightDocument` 定义独立的 `adv_py_skin_weights` schema v1，保存 skinCluster 短名、mesh 完整路径、顶点数、完整 influence 路径集合、最大影响数设置和全部顶点的规范化非零权重。序列化前要求顶点索引无缺口地覆盖 `[0, vertexCount)`，每个顶点继续复用上一切片的权重验证。文档内容使用稳定键序列化后计算 SHA-256；读取端拒绝未知/缺失字段、错误类型、未知 schema、非法权重和摘要不匹配。

`ExportSkinWeights` 读取 Maya skinCluster 的全部顶点，不依赖选择；目标必须是尚不存在的 `.json`，且父目录已存在。文件先写入同目录临时文件并 flush/fsync，重新解析与文档逐字段一致后再原子替换，异常时清理临时文件。`ImportSkinWeights` 先核对文档与目标场景的 skin、mesh、顶点数、完整 influence 集合、最大影响数及锁权重；执行前再次确认文件和全网格场景快照未变化，再把全部顶点交给 `EditSkinWeights`，因此只有实际不同的顶点进入一个 Undo Chunk。

Maya 2024 隐藏独立进程 PID 28344 验证确认：72 个顶点全部导出为 24,838 字节 JSON；已有目标文件不会被覆盖。改写 3 个顶点后导入准确恢复，重复导入为零变化，一个 Undo 恢复导入前权重并保留 skinCluster；临时目录和场景节点全部清理。本切片只支持精确场景路径与拓扑，不提供 namespace/influence 映射、部分文档、合并策略、压缩格式、网络盘原子性承诺或 Blender 适配。

## Maya 切片 40：Skin Weight 显式路径映射

`SkinWeightPathMapping` 为一次导入显式声明目标 skinCluster、目标 mesh，以及每个源 influence 到目标 influence 的映射。核心层要求源集合与文档 influence 集合完全一致且无额外项，源路径不能重复，目标路径也不能重复，从而形成完整双射。`remap_skin_weight_document` 重写文档级 influence 顺序和每个顶点的权重路径，再调用现有完整顶点、归一化、最大影响数与 SHA-256 生成流程；源文档保持不变。

`ImportSkinWeights.plan(..., mapping=...)` 使用映射后的目标文档执行 topology、influence、锁权重和最大影响数预检；apply 阶段分别复检原始文件未变化、目标全网格快照未漂移，再由 `EditSkinWeights` 只提交实际差异。映射对象不会读取 Maya namespace，也不会按短名、后缀、左右侧或层级相似度猜测目标，因此错误对应必须由调用方在修改前显式修正。

Maya 2024 隐藏独立进程 PID 43744 使用两套路径完全不同的 8 顶点网格、skinCluster 和 joint 集合验证：2 条 influence 双射把源文档重写到目标，实际导入 3 个变化顶点；再次导入为零变化，源权重始终不变。一个 Undo 恢复目标导入前权重并保留两套 skinCluster，临时文件和场景节点全部清理。本切片不实现自动 namespace 推断、模糊名称匹配、拓扑重采样、左右镜像、部分合并或 Blender 适配。

## Maya 切片 41：显式 Skin Weight 镜像

`SkinWeightMirrorRequest` 为同一 skinCluster/mesh 声明显式 `sourceIndex → targetIndex` 顶点对和 `sourceInfluence → targetInfluence` 映射。源与目标顶点集合必须各自唯一且互不重叠，避免镜像提交时覆盖尚未读取的源；influence 源唯一、目标也必须一一对应。中心 joint 等不换侧的 influence 可声明身份映射。核心审计读取指定源/目标顶点，检查索引、skin/mesh、influence 成员、目标锁权重，并拒绝任何实际非零源权重缺少映射。

`plan_skin_weight_mirror` 只根据源快照和显式映射生成目标顶点的完整非零权重，再复用 `skin_weight_request` 的归一化合同。`MirrorSkinWeights` 在预演中把目标交给 `EditSkinWeights` 计算 before/after；apply 前同时复检源和目标快照未变化，最终只有实际不同的目标顶点进入既有单事务写入与复检。重复镜像不创建空 Undo，源顶点始终只读。

Maya 2024 隐藏独立进程 PID 42860 在 8 顶点合成对称平面上验证：2 个顶点对通过 Right→Left 两条映射与一条中心身份映射得到目标权重。移动目标 Left influence 后目标顶点最大位移为 0.7，源顶点位移为 0；重复镜像为零变化。一个 Undo 恢复目标镜像前权重，源权重与 skinCluster 保持，最后无残留节点。本切片不计算空间对称关系、不猜测左右命名，也不包含拓扑不对称重采样、混合/平滑策略或 Blender 适配。

## Maya 切片 42：严格几何配对的 Skin Weight 镜像

`SkinWeightGeometryMirrorRequest` 在显式 skinCluster、mesh 和 influence 双射之外，要求调用方给出 `negative_to_positive` 或 `positive_to_negative` 方向、世界空间 X 镜像平面原点与正有限容差。`plan_skin_weight_geometry_pairs` 检查 mesh 身份、完整顶点索引和有限世界位置；中心容差带内的顶点保持不变，平面两侧顶点必须数量相同，且每个源点反射后只能匹配一个未使用的目标点。缺点、多候选、目标复用或残余目标都会形成稳定 issue，不产生部分顶点对。

`MirrorSkinWeightsByGeometry` 先通过 Maya adapter 只读采集单一 polygon transform 的全部顶点位置，再把生成的配对交给上一切片的 `MirrorSkinWeights`。因此 influence 成员、锁权重、未映射非零权重、最大影响数、before/after 计划、单次 Undo 和逐顶点结果复检都继续使用同一合同；执行前同时复查几何与源/目标权重快照，重复执行不会创建空事务。该入口不依赖当前选择，也不按名称猜测左右侧。

Maya 2024 隐藏独立进程 PID 8032 在代码生成的 8 顶点对称平面上读取完整世界位置，并从负 X 到正 X 生成 4 个一一对应顶点对。3 条显式 influence 映射完成全部目标权重替换；移动目标 Left influence 后目标侧最大位移为 0.7，源侧位移为 0。选择保持，重复执行为零变化，一个 Undo 恢复镜像前目标权重并保留源权重与 skinCluster，清理后无残留进程或节点。本切片只接受严格位置对称，不提供拓扑映射、近似最近点、非对称重采样、自动 influence 配对或 Blender 实现。

## Maya 切片 43：Arm 轴向 swing/twist 分解

切片 36 的 helper 使用双端 `parentConstraint` 插值完整旋转，因此 Wrist 或 Elbow 的非轴向 swing 也会进入中间 deformation joints。本切片把合同拆成两个正交部分：`pointConstraint` 只负责按 fraction 插值 Shoulder→Elbow 或 Elbow→Wrist 的世界位置；每段独立基座只跟随起点世界变换，helper 作为基座子级只允许局部 rotateX。`project_twist_quaternion_x` 在 DCC 无关核心中把局部旋转四元数 `(x, y, z, w)` 规范化投影为 `(x, 0, 0, w)`，纯 swing 和 180° swing 的退化投影都定义为单位四元数。

Maya adapter 使用内置 `composeMatrix → decomposeMatrix → quatToEuler` 网络：端点 rotate 与 rotateOrder 先组成不含 jointOrient 的动画局部旋转，随后只把 `outputQuatX/outputQuatW` 接入轴向投影。每个 helper 通过独立 `unitConversion` 按 fraction 缩放投影角度并驱动 rotateX，rotateY/rotateZ 保持零。四个段基座、四套投影网络、八个位置约束和八个角度比例节点都进入名称预检与结构复检；`quatNodes` 是 Maya 随附依赖，在打开场景事务前准备，加载失败时不创建任何 Rig 节点。整套网络继续位于 `BuildBodyArmRig` 的单一事务内。

首次隔离验证发现审计把 `unitConversion.output` 错写为 `outputX`，完整 Arm Rig 在 twist 阶段自动回滚且进程正常退出。修正合同后，Maya 2024 隐藏独立进程 PID 41060 验证确认：Wrist 局部 X 旋转 72° 时，下臂两个 helper 分别得到 24° 和 48°；单独旋转 Y 55° 时轴向投影与 helper 三轴旋转均为 0。手臂伸长到 1.25 倍后全部 helper 仍位于端点声明插值位置；选择保持，一个 Undo 移除完整 Arm Rig 并恢复 Body，清理后无残留节点或进程。本切片不包含体积保持、全局缩放补偿、stretch 状态下的 IK→FK 匹配、腿部 twist 或 Blender 实现。

## Maya 切片 44：Arm stretch 横向体积保持

`BodyArmVolumePlan` 消费第 35 切片已经过审计的每侧最终 IK stretch ratio、第 28 切片的同侧 `armIkFk_R/L` 模式属性，以及第 43 切片的 twist helper 集合。DCC 无关函数 `volume_preservation_scale` 把长度比例 `r` 转换为横向比例 `r^-0.5`，使长度与横截面积乘积近似保持；每侧 `armVolume_R/L` 强度在 0–1 内线性混合横向比例 1 和该守恒值。FK 模式把有效比例固定为 1，避免隐藏 IK control 的位置影响当前 FK 形变；切到 IK 后才引入 stretch ratio。无效比例、越界强度、缺失 stretch/blend 侧或空 helper 集合会在生成场景计划前失败。该合同不改变 Body 或 mechanism joint 的 scaleX，也不把 squash、肌肉形变或网格体积测量混入本切片。

Maya adapter 在 twist helper 构建并复检后创建两套 `blendColors(mode: 1↔IK ratio) → multiplyDivide(power=-0.5) → blendColors(strength)` 网络。stretch ratio 与 `armIkFk_R/L` 先产生当前模式的有效比例，最终输出只连接同侧 helper 的 scaleY/scaleZ；默认强度 1，FK 或绑定比例 1 时所有缩放保持 1。模式输入、属性、运算模式、指数、基准值、左右输入和每个 helper 的两个目标插头全部进入核心快照与复检。任何同名节点、缺失 ratio/helper、错误连线或后检失败都会从 `BuildBodyArmRig` 的同一个 Undo Chunk 回滚此前 mechanisms、controls、IK、stretch 和 twist 产物。

Maya 2024 隐藏独立进程 PID 40040 验证确认：FK 模式下先把隐藏 Wrist IK 移到 1.44 倍目标，4 个右侧 helper 的 Y/Z 比例仍为 1；切到 IK 后才变为 0.833333。`armVolume_R` 为 0 时恢复 1，为 0.5 时得到 0.916667；左侧 helper 保持 1，右侧轴向 twist 继续有效，选择保持。一个 Undo 移除完整 Arm Rig 并恢复 Body，清理后无残留节点或进程。本切片不包含全局缩放补偿、压缩 squash、逐段独立强度、stretch 状态下的 IK→FK 匹配、腿部或 Blender 实现。

## Maya 切片 45：stretch 状态下的 Arm IK→FK 匹配

`BodyArmIkToFkPlan` 在原有三层 FK control 世界轴之外，计算当前 Body 的 Shoulder→Elbow 与 Elbow→Wrist 向量在父关节本地 X 轴上的有符号投影，并把对应 FK driver 的两个 `translateX` 作为显式输出。生成计划时要求两段仍沿父级本地 X 对齐；控制旋转、两个段长通道和目标侧 blend 必须全部存在且可写。预检快照同时保存当前 Body 三关节位置、世界轴与 FK 段长，进入事务后完整重采，避免预演和提交之间的姿态漂移。

Maya adapter 在同一个 Undo Chunk 内按父子顺序匹配三层 FK control 世界轴，写入两段 FK driver 长度，最后把目标侧 `armIkFk` 切到 0。提交后复检三关节位置与朝向、两个段长值、目标及另一侧 blend、Fit 输入和用户选择；任一阶段失败都会恢复 IK 模式及之前的 FK 通道值。FK control 的 translate 继续保持零，段长由机制层承载，不把位置补偿混入控制层。

Maya 2024 隐藏独立进程 PID 12372 验证确认：右 Wrist IK 被移到绑定总长的 1.35 倍后，IK→FK 写入两段 FK driver 长度 4.05 与 3.375；切换前后三关节世界位置和朝向保持，左侧 blend 不变，FK controls 正常显示且 translate 为零。一次 Undo 恢复 IK 模式和拉伸姿态，第二次 Undo 移除完整 Arm Rig；选择保持，清理后无残留节点或进程。本切片不包含全局缩放补偿、非均匀/逐段 IK stretch、动画 bake、腿部或 Blender 实现。

## Maya 切片 46：Arm stretch 显式全局比例补偿

`BodyArmStretchPlan` 增加共享的正值 `armGlobalScale` 输入，每侧增加独立参考长度节点。DCC 无关函数 `compensated_stretch_ratio(distance, rest_length, global_scale)` 定义比例为 `distance / (rest_length × global_scale)`，并拒绝零值、负值与非有限输入。该比例只校正 stretch 的测距基准：两侧强度、no-compression、IK/FK 模式和 volume 继续消费同一条最终比例，不把全局比例再次乘进关节段长。

Maya adapter 用 `multiplyDivide` 把绑定长度与 `armGlobalScale` 相乘，再把结果接入现有 distance ratio 的除数。属性默认 1、最小值 0.0001，可由未来角色总控的统一 scale 通道直接连接；当前切片不创建或猜测总控。全局属性、两套参考长度节点、常量、操作模式和全部输入连线进入构建后快照与审计，同名节点、已有属性、非法输入或错误连线会随完整 Arm Rig 的单一事务回滚。

Maya 2024 隐藏独立进程 PID 28896 验证确认：外部 transform 的 `scaleX` 可连接到 `armGlobalScale`；当世界目标距离和全局比例同为绑定态的 1.35 倍时，IK 两段与 volume 均保持比例 1，不产生假拉伸。维持 1.35 全局比例并把目标增加到补偿后长度的 1.25 倍时，两段正确伸长 1.25 倍，helper 横向比例变为 `1/sqrt(1.25)`。原有 no-compression、强度混合、左右隔离、选择保持与一次 Undo 全部通过，清理后无残留节点或进程。本切片不建立完整角色总控层级、不支持非均匀缩放，也不扩展腿部或 Blender。

## Maya 切片 47：双腿 FK/IK mechanism chains

Arm 与 Leg 的共同语义下沉为 `BodyLimbMechanismPlan`：调用方必须提供稳定的 limb 标签和恰好三个互不重复的 joint 名称，通用核心负责生成双侧 FK/IK 规格、快照与审计。既有 Arm 公共 API 保留为薄兼容包装，因此本次抽取不改变已经验证的 Arm Rig 行为。

`BodyLegMechanismPlan` 固定使用 Hip/Knee/Ankle，`BuildBodyLegMechanisms` 在提交前核对 Body provenance、ReBuild 安全状态与完整名称集合。Maya adapter 复用相同的 joint 创建、`jointOrient`、side label、Body source message 连线和快照读取路径；12 个驱动 joint 在一个 Undo Chunk 内提交，完成后重新核对 Body、Fit、settings 和用户选择，任何异常都会回滚。

首次隐藏 Maya 2024 验证进程 PID 30812 的场景行为和清理均正确，但验证脚本把核心 side 枚举值误写为 `right/left`，因此以断言失败退出；修正为稳定合同值 `R/L` 后，纠正性进程 PID 7948 全部通过：12 个 joint、左右各 6 个、FK/IK 各 6 个、来源连线、链间独立运动、ReBuild 阻止、单次 Undo 恢复、选择保持与清理均得到确认。本切片不包含腿部 controls、RP IK、Pole Vector、blend、Foot、stretch、twist 或 Blender 实现。

## Maya 切片 48：双腿 FK mechanism controls

Arm 与 Leg 的控制规格下沉为 `BodyLimbFkControlPlan`：通用核心接收稳定的 limb 标签、恰好三个互不重复的 Body joint 名称、控制半径和可选的 source→driver 显式映射，生成双侧三级 offset/control 层级、NURBS 控制规格与 orientConstraint 规格。审计统一核对节点集合、父链、曲线类型、世界帧、零通道和 source/driven 连线；既有 `BodyArmFkControl*` API 作为兼容别名及薄包装保留，完整 Arm Rig 定向回归通过。

`BuildBodyLegFkMechanismControls` 先从 Fit 对称计划推导 Body provenance，再读取并审计现有 12 个 Leg mechanisms，只把其中 6 个 FK driver 映射给 Hip/Knee/Ankle controls。名称冲突、Body 身份异常或 mechanism 漂移均在写入前阻止；Maya adapter 复用通用曲线、offset、orientConstraint 与快照路径，在一个 Undo Chunk 内建立控制层，并复检 Body、全部 Leg mechanisms、Fit 和 settings 未被改写。

Maya 2024 隐藏独立进程 PID 40900 验证确认：6 个控制通道归零且只连接 FK drivers；旋转右 Hip FK control 会移动 FK Ankle，而 IK Ankle 与 Body Ankle 保持不变。第一次 Undo 只移除控制层并保留 12 个 mechanisms，第二次 Undo 移除 mechanisms 后 Body 恢复 ReBuild 安全；用户选择、Fit、Body 和无关节点均保持，清理后无 Maya/mayapy 残留。本切片不包含 Leg RP IK、Pole Vector、FK/IK blend、Foot、stretch、twist 或 Blender 实现。

## Maya 切片 49：双腿 RP IK controls

三关节 Pole Vector 几何下沉为 `solve_limb_pole_position`，显式接收 start/middle/end、备用轴、limb 标签和距离比例，并验证有限向量、非重合端点及备用方向。Arm 的 `solve_arm_pole_position` 保留为兼容包装。Leg 的端点语义独立建模为 Ankle，不复用 Wrist 字段，从而为后续 Foot roll、Ball/Toe 层级和不同朝向策略保留边界。

`BodyLegIkPlan` 从已验证 Body 和 Leg mechanism 规格建立左右 Ankle/Pole Vector offsets、controls、RP IK Handles、poleVectorConstraints 与 Ankle orientConstraints。完全笔直的腿没有几何弯曲平面，规划器会从 Knee 的三条世界轴中确定性选择与 Hip→Ankle 最不平行的轴。`BuildBodyLegIkControls` 在写入前核对 Body provenance、完整 12-joint mechanism 快照和全部名称，在一个 Undo Chunk 内创建并复检控制层；Body、Fit、settings 及机制链结构必须保持不变。

Maya 2024 隐藏独立进程 PID 42688 验证确认：左右 Ankle/PV controls 和 `ikRPsolver` 创建成功，移动右 Ankle control 后 IK Ankle driver 到达目标，Body Ankle 保持不变；Pole Vector 与 Ankle 朝向 source 连线正确。第一次 Undo 只移除 IK 控制层并保留 mechanisms，第二次 Undo 移除 mechanisms 后 Body 恢复 ReBuild 安全；选择、Fit、Body、无关节点与清理均通过。本切片不包含 Leg FK/IK blend、Foot、stretch、twist、match 或 Blender 实现。

## Maya 切片 50：双腿 rotation/translation blend

Arm 与 Leg 的输出规格下沉为 `BodyLimbBlendPlan`：通用核心接收 limb 标签、三关节名称和需要位移混合的关节集合，建立每侧独立的 0–1 属性、reverse、FK/IK 双源旋转约束与可选双源位移约束。既有 `BodyArmBlend*` API 保留为兼容包装；Leg 固定使用 Hip/Knee/Ankle，其中 Hip 只混合旋转，Knee 与 Ankle 同时混合旋转和位置，使 IK solver 或未来 stretch 的段长结果能够到达最终 Body。

`BuildBodyLegBlend` 在写入前核对 Body provenance、完整 Leg mechanism 快照及 settings/reverse/constraint 名称集合。Maya adapter 复用同一套 DG 创建和快照读取路径，建立 `legIkFk_R/L`、两侧 reverse、6 个 orientConstraint 与 4 个 pointConstraint；构建后审计 target 顺序、FK/IK 权重来源、driven Body joints、属性初值和 Fit 输入，任一异常由单一 Undo Chunk 回滚。

Maya 2024 隐藏独立进程 PID 47964 验证确认：FK=0 时右 Hip control 通过 FK mechanisms 驱动 Body，IK=1 时右 Ankle control 通过 RP IK 驱动 Body Ankle；0.5 时右侧全部旋转和位移约束权重均为 0.5，左侧属性与姿态保持不变。第一次 Undo 移除后建的 IK 层，第二次 Undo 单独移除 blend，FK controls、mechanisms、Body、Fit 与无关节点仍存在；最终清理无残留。本切片不包含 Leg 模式显隐、原子完整 Leg Rig、Foot、match、stretch、twist 或 Blender 实现。

## Maya 切片 51：双腿 FK/IK 模式显隐

Arm 与 Leg 的共同模式显示规则下沉为 `BodyLimbVisibilityPlan`：每侧唯一的 FK 根 offset 接受 reverse 输出，两个 IK 根 offset 接受该侧 0–1 blend 属性。Arm 保留 Wrist/PV 包装，Leg 独立映射 Ankle/PV，因此核心规则可移植而宿主语义不会混淆。本次同时让 mechanism 审计支持“只检查结构、不要求绑定初始姿态”，供已经进入真实 IK/FK 姿态的后续用例复核父链、side 与 Body 来源，默认严格行为保持不变。

`BuildBodyLegVisibility` 只消费已经独立构建并通过结构审计的 Body、Leg mechanisms、FK controls、Ankle/PV IK controls 与 blend。预演读取每个源属性和六个目标 visibility，缺失源、锁定目标或已有输入会在事务前停止；提交后重新读取全部连接，任一侧错误都会撤销本次所有连线。它不重复创建上述节点，也不把完整 Leg Rig、Foot、match 或 stretch 偷渡进本切片。

Maya 2024 隐藏独立进程 PID 29624 验证确认：预演不修改场景，默认 `legIkFk=0` 时左右都只显示 Hip FK 层级；把右侧切到 1 后右 Ankle/PV 显示且右 FK 隐藏，左侧继续保持 FK。当前选择不变，一个 Undo 只移除六条显隐连接，FK、IK、blend、mechanisms、Body 与 Fit 均保留，清理后无残留节点或 Maya/mayapy 进程。本切片不包含原子完整 Leg Rig、Foot roll、无跳变匹配、stretch、twist、volume 或 Blender 实现。

## Maya 切片 52：原子基础双腿 Rig

`BodyLegRigBuildPlan` 从同一份经过 ReBuild 安全审计的 Body 快照一次生成 mechanisms、FK controls、rotation/translation blend、Ankle/PV IK 与模式显隐计划，并在写入前汇总全部根节点、joint、control、constraint、IK Handle、reverse 和设置节点名称。预演只读，不要求调用方先创建任何中间产物；已有 Rig、用户同名节点或不安全 Body 外部依赖都会在事务开始前阻止构建。

`BuildBodyLegRig` 严格按 mechanisms → FK → blend → IK → visibility 的顺序在同一个 Undo Chunk 内提交。blend 先于 IK 创建，使 Body 在默认 FK=0 下保持绑定姿态；每个阶段都立即读取真实场景并使用既有核心审计复检，最后再次核对 Body 绑定姿态以及 Fit/settings 未改变。通用 `body_bind_pose_matches` 从 Arm 私有实现提取为 application 层共享校验，不引入 Maya API，也不改变已验证的 Arm Rig 行为。末阶段失败会由宿主事务移除之前创建的所有 Leg 节点。

Maya 2024 隐藏独立进程 PID 39432 验证确认：预演不修改场景，单次构建得到 12 个 mechanism joints、6 个 FK controls、双侧 Ankle/PV IK、6 个 Body blend 约束和两侧显隐。右 Hip FK 与右 Ankle IK 分别在对应模式真实驱动 Body，切换右侧时左腿姿态与显示保持不变；用户选择不变。一个 Undo 移除全部 `AdvPy_Leg*` 产物，Body 与 Fit 恢复原快照且 ReBuild 再次安全，无关节点保留，最终无节点或 Maya/mayapy 进程残留。本切片不包含 Foot、无跳变匹配、stretch、twist、volume 或 Blender 实现。

## Maya 切片 53：Leg FK→IK 无跳变匹配

`BodyLegFkToIkPlan` 从目标侧当前 Hip/Knee/Ankle Body 快照读取三关节位置与 Ankle 世界轴，把 Ankle IK control 的完整平移/旋转通道、Pole Vector 平移通道和 `legIkFk` 属性声明为唯一写入集合。Pole Vector 由 DCC 无关的三点投影算法计算；直腿退化时，从 Knee 的三条世界轴中选择与 Hip→Ankle 最不平行的一条作为确定性备用方向。该模型使用独立 Ankle 语义，不把 Arm 的 Wrist 字段或后续 Foot 规则混入本切片。

`MatchBodyLegFkToIk.plan` 核对 Body provenance、现有 Leg IK 与 blend 结构、目标侧 FK 模式、节点存在性及通道可写性，同时把当前三关节位置和 Ankle 世界轴纳入场景状态。apply 在打开 Undo Chunk 后完整重采该状态，拒绝预演后发生的姿态漂移；随后对齐 Pole Vector、写入 Ankle IK 世界矩阵并最后切换 blend。构建后复检 Hip/Knee/Ankle 位置、Ankle 朝向、目标模式、另一侧 blend 和 Fit/settings，任何异常恢复匹配前状态。

Maya 2024 隐藏独立进程 PID 11004 验证确认：右 Hip/Knee/Ankle FK controls 形成非绑定弯腿与 Ankle 旋转姿态后，匹配切换保持三关节位置和 Ankle 世界轴；右侧 Ankle/PV 显示，左腿姿态、blend 与 FK 显示不变，用户选择保持。一个 Undo 恢复右侧 FK 模式和匹配前姿态，第二个 Undo 移除基础 Leg Rig，最终无节点或 Maya/mayapy 进程残留。本切片不包含 Leg IK→FK、Foot、stretch、twist、volume、动画 bake 或 Blender 实现。

## Maya 切片 54：Leg IK→FK 固定链长无跳变匹配

`BodyLegIkToFkPlan` 从目标侧当前 Hip/Knee/Ankle Body 快照记录三层 FK control 的目标世界轴、三关节位置与世界轴，并把九个 FK rotate 通道和同侧 `legIkFk` 声明为唯一写入集合。现阶段 Leg mechanism 没有 stretch，因此计划不会把当前 IK 段长写入 FK driver translate；这使合同只承诺固定链长可达姿态，不用尚未存在的 Leg stretch 能力伪装完整匹配。

`MatchBodyLegIkToFk.plan` 核对 Body provenance、现有 FK control 与 blend 结构、目标侧 IK 模式、节点存在性和通道可写性，同时保存完整三关节姿态用于提交前漂移检查。Maya adapter 按 Hip→Knee→Ankle 父子顺序保留每个 control 的世界位置并写入目标世界轴，最后切换 blend。提交后逐关节复检位置与朝向，并确认另一侧 blend、Fit/settings 和用户选择不变；任一问题由同一 Undo Chunk 恢复。

Maya 2024 隐藏独立进程 PID 35976 验证确认：右 Ankle IK 移到可达的非绑定位置、Pole Vector 改变且 Ankle control 带 X/Z 旋转后，切回 FK 保持 Hip/Knee/Ankle 的位置与全部世界轴；三层 FK control 本地 translate 继续为零，左腿姿态、blend 和显示不变。一个 Undo 恢复原 IK 模式与姿态，第二个 Undo 移除基础 Leg Rig，最终无节点或 Maya/mayapy 进程残留。本切片不覆盖拉伸姿态、Foot、twist、volume、动画 bake 或 Blender 实现。

## Maya 切片 55：Foot pivot 与显式 roll/bank 基础

`BodyLegFootPlan` 独立消费已验证 Body 与 Leg IK 规格，把每侧 Heel、FootSideOuter、FootSideInner、ToesEnd、Toes 的世界位置映射为 Heel→Outer→Inner→Toe→Ball 五级 reverse-foot pivot。核心层显式记录 pivot 角色、父链、位置、控制属性、旋转轴、驱动 plug 和 inner-bank 负号，不依赖 `maya.cmds`；本切片使用现有 Body 的局部 X 前向约定，让 roll 驱动本地 Y、bank 驱动本地 X。它没有从授权 MEL 复制过程、常量或实现片段。

`BuildBodyLegFoot` 只扩展已经存在的原子基础 Leg Rig。预演核对 Body provenance、Leg IK 结构、Ankle control 与 RP IK Handle 的原父级，并拒绝 pivot/utility 同名节点、既有五个属性以及锁定或引用输入；提交时在一个 Undo Chunk 内添加 `heelRoll/outerBank/innerBank/toeRoll/ballRoll`，创建双侧 10 个 pivot 和两个 inner-bank 符号节点，再以保持世界变换的方式把 Handle 挂到 Ball pivot。复检覆盖属性默认值、完整父链、标记位置、真实上游连线与 Handle 父级；任何一侧失败都会恢复属性、节点和原 Handle 层级。Leg FK→IK 的结构审计只在该匹配用例中允许受支持的 Foot Handle 父级扩展，其他构建入口仍保持严格默认审计。

Maya 2024 隐藏独立进程 PID 10464 验证确认：预演零修改，双侧 10 个 pivot 与五个通道均创建成功，五个右侧通道分别能真实移动 RP IK Handle，inner bank 正值经过 -1 符号节点，Foot 建成后 FK→IK 匹配仍可通过预检，用户选择保持。第一次 Undo 只移除 Foot 并把 Handle 恢复到 Ankle control，第二次 Undo 移除基础 Leg Rig，最终清理无残留。本切片是显式 pivot 基础，不包含自动分段 `footRoll`、独立 Toe IK/Toe mechanism、Leg stretch、twist、volume、动画 bake 或 Blender 实现。

## Maya 切片 56：Foot 纳入原子 Leg Rig 主流程

`BodyLegRigBuildPlan` 现在从同一份 Body 安全快照同时生成 Foot 计划，并把 10 个 pivot 与两个 inner-bank utility 名称加入原有 Leg 根节点、mechanism、control、constraint、Handle 和设置节点的统一碰撞预检。该编排不重新实现 Foot 业务规则，也不把 Maya API 放进核心；独立 `BuildBodyLegFoot` 仍可服务由分阶段用例组成的 pre-Foot Rig，默认成品入口则不再要求调用方额外执行第二个事务。

`BuildBodyLegRig.apply` 的提交顺序扩展为 mechanisms → FK → blend → IK → visibility → Foot。Foot 位于最后阶段，创建后使用 `BodyLegFootSnapshot` 复检双侧属性、层级、标记位置、上游连线和 Handle 父级，再重新采集最终 IK 快照，确认除受支持的 Handle 父级扩展外，RP solver、Pole Vector 和 Ankle 朝向结构没有漂移。Foot 失败会由同一个宿主事务撤销此前所有 Leg 阶段；成功结果返回最终 Foot 与重新采集的 IK 状态，避免向调用方暴露 Foot 创建前的过期 Handle 路径。

Maya 2024 隐藏独立进程 PID 45356 验证确认：只读预演后，一次调用创建 12 个 mechanism joints、6 个 FK controls、双侧 Ankle/PV IK、blend/显隐以及 10 个 Foot pivots；五个右侧 roll/bank 通道均真实移动 RP IK Handle，inner bank 符号、Foot 后 FK→IK 预检和选择保持通过。一个 Undo 同时移除全部 Leg/Foot 产物，清理后无残留；启动前已有 Maya/mayapy PID 保持不变，本轮进程自行退出。本切片不增加自动 `footRoll`、Toe mechanism/IK、Leg stretch、twist、volume、动画 bake、UI 或 Blender 功能。

## Maya 切片 57：五关节 Leg mechanisms 与 Toes FK/output

通用 `BodyLimbMechanismPlan`、`BodyLimbFkControlPlan` 和 `BodyLimbBlendPlan` 从固定三关节定义收敛为至少两个、名称唯一且父链连续的可变长度合同；Arm 包装继续传入 Shoulder/Elbow/Wrist，因此既有 Arm 产物数量和行为不变。Leg 包装改为生成 Hip→Knee→Ankle→Toes→ToesEnd 的 FK/IK mechanisms，控制层覆盖 Hip/Knee/Ankle/Toes，输出层覆盖 Hip/Knee/Ankle/Toes；ToesEnd 作为机制链末端和朝向基准，不创建无业务意义的末端控制或 Body 约束。

原子 Leg Rig 因此创建 20 个 mechanism joints、8 个 FK controls、8 个双源旋转 blend 和原有 4 个 Knee/Ankle 位移 blend。Toes FK control 是 Ankle FK control 的子层，只约束对应 Toes FK driver；Body Toes 通过现有 `legIkFk_R/L` 在 FK/IK Toes driver 间切换，ToesEnd 与足侧分支自然继承 Body Toes 输出。本切片没有伪造不存在的 Toe IK control：FK→IK 预检比较 Body 与 IK mechanisms 的 Ankle→Toes 相对朝向，非中性 Toes FK 姿态无法表示时在事务前拒绝；IK→FK 则把第四层 Toes FK control 一并匹配并复检四关节姿态。

Maya 2024 最终纠正性隐藏进程 PID 13760 验证确认：原子 Rig 的 20 个 mechanisms、8 个 controls、8 个旋转 blend、Foot 与显隐均通过；旋转右 Toes FK control 会真实移动 Body ToesEnd，左侧保持不变。中性 Toe 姿态的 FK→IK 保持 Hip/Knee/Ankle/Toes 位置及 Ankle/Toes 末端朝向，IK→FK 保持进入该切换前的四关节位置和世界轴；一个 Undo 移除完整 Leg/Foot Rig，Body/Fit/ReBuild 安全和清理均通过。首次验证 PID 45948 及两次诊断进程仅暴露烟雾脚本使用了超出既有匹配合同的错误比较基准，场景构建、Undo 和清理均通过；最终脚本已按各用例实际保证的姿态范围修正。本切片不包含独立 Toe IK、Foot 输出朝向、自动 `footRoll`、Leg stretch/twist、动画 bake、UI 或 Blender 功能。

## Maya 切片 58：Foot 到 Ankle/Toes IK 的朝向输出

`BodyLegIkSpec` 现在显式携带同侧 Toes IK driver，`BodyLegFootSideSpec` 则声明原 Ankle 约束、新 Toes 约束以及 Ball/Toe 两个朝向来源。Foot 输入预检不仅扫描 pivot、utility 和新约束名称，还确认原 Ankle orientConstraint 的唯一 source/driven、相关节点可写，并拒绝 Toes IK rotate 上的既有输入；不会删除未知约束或覆盖用户连线。核心 Foot 快照新增两条朝向约束的名称、来源和目标，运行态审计可选择忽略动画属性值与已旋转 pivot 的初始位置，但仍严格核对结构和接线。

Maya adapter 在同一个 Foot/完整 Leg Rig 事务内，把原 Ankle control→Ankle IK driver 约束安全替换为 Ball pivot→Ankle IK driver，并以保留绑定朝向偏移的 Toe pivot→Toes IK driver 约束补齐脚趾输出。完整 Leg Rig 的后置 IK 审计不再使用宽泛的“忽略 Handle 父级”开关，而是逐侧传入精确的 Ball 父级与 Ankle 来源；FK→IK 预检同时验证完整 Foot 快照。因此损坏的 Foot 约束会在匹配写入前停止，事务失败或一次 Undo 会一并恢复原约束和层级。

Maya 2024 隐藏独立进程 PID 19112 验证确认：双侧 10 个 pivot、五个显式 roll/bank 通道、Handle 父级和两条朝向输出均通过；`ballRoll` 会旋转 Ankle IK driver 并保持 Toes 世界朝向，`toeRoll` 会同时旋转 Ankle 与 Toes IK drivers。中性状态下 FK→IK 仍可预检，选择保持，一个 Undo 移除完整 Leg/Foot Rig，清理无残留；该验证进程自行退出。本切片不实现 Foot-aware FK→IK 属性求解、自动分段 `footRoll`、Leg stretch/twist、动画 bake、UI 或 Blender 功能。

## Maya 切片 59：三轴 Toe IK control 与 Foot-aware FK→IK

最初尝试把任意 Toes FK 相对朝向反求为单一 `ballRoll`。真实 Maya 预检证明该假设不成立：Ball pivot 的旋转帧与实际 Toes FK 局部帧并非普遍相同，单轴值不能精确表达三轴 Toe 姿态。实现因此改为在每侧 Toe pivot 下建立 `AdvPy_ToeIKOffset_R/L → AdvPy_ToeIK_R/L`，offset 使用 Body Toes 的绑定世界帧，control 保持零通道并通过独立 orientConstraint 直接驱动 Toes IK driver。Toe control 继承 Heel/Bank/Toe pivots，Ball pivot 仍只影响 Ankle 与 RP Handle，原有 planted-toe 行为保持。

`BodyLegFootSideSpec` 与快照现已声明 Toe offset/control 的名称、完整父链、世界帧、半径、NURBS shape、零通道和约束 source/driven；完整 Rig 与独立 Foot 用例把新增名称纳入碰撞预检，失败仍由原事务撤销。`BodyLegFkToIkPlan` 把 Toe control 三个 rotate 通道和五个 Foot 属性加入唯一写入集合，并把当前 Toes Body 世界轴加入提交前漂移快照。apply 先归零目标侧 Foot 通道，再匹配 Pole Vector、Ankle control 和 Toe control，最后切换 blend；构建后复检 Hip/Knee/Ankle/Toes 位置与朝向、目标 Foot 值、另一侧 blend/Foot 以及 Fit 输入。

第一次隐藏验证 PID 40512 按预期拒绝了旧的单轴 `ballRoll` 方案，场景未提交且进程自行退出。修正后的 Maya 2024 隐藏进程 PID 23828 使用右侧 Hip/Knee/Ankle 复合姿态和 Toes FK `rotateY=28°` 验证：预演零修改，三轴 Toe IK control 被纳入匹配，切换后四关节姿态保持、目标 Foot 通道归零、左右侧状态和选择不变；一次 Undo 恢复 FK 模式、姿态与 Foot 值，第二次 Undo 移除完整 Leg Rig，清理无残留。本切片不实现自动分段 `footRoll`、Leg stretch/twist、动画 bake、UI 或 Blender 功能。

## Maya 切片 60：可叠加的自动分段 footRoll

现有 MEL 清单没有提供可独立复刻的 `footRoll` 过程边界，因此本切片采用 clean-room 语义合同，不复制原始脚本表达。纯 Python `segmented_foot_roll` 把输入限制在 `[-360°, 360°]`：负值只输出 Heel；`0°–45°` 只输出 Ball；超过 `45°` 后 Ball 保持在 45°，余量输出到 Toe。非有限数值、无效范围和不在范围内的 break angle 会在创建 Maya 节点前拒绝。

每侧使用 4 个分段节点与 3 个加法节点：Heel/Ball clamp、Toe subtract/clamp 分别产生自动输出，Heel/Toe/Ball sum 再把自动值与已有 `heelRoll/toeRoll/ballRoll` 相加。`outerBank/innerBank` 和三轴 Toe IK control 的既有行为不变。`BodyLegFootRollSpec` 与运行快照显式记录主属性、上下限、break angle、节点类型、数值和每条连接；Foot 名称预检、构建后审计、完整 Leg Rig 的统一碰撞扫描、失败回滚和一次 Undo 均覆盖这些节点，不依赖选择或 UI。

Maya 2024 隐藏独立进程 PID 33832 验证确认：预演零修改，`footRoll=-30°` 只驱动 Heel，`20°` 只驱动 Ball，`70°` 得到 Ball 45° 与 Toe 25°，手动 `ballRoll=5°` 可在自动 Ball 输出上叠加；六个 Foot 通道均能真实影响 Handle，Ball/Toe 朝向输出、FK→IK 预检、选择保持、单次 Undo 和最终清理全部通过，进程自行退出。当前 break angle 固定为 45°，尚未提供角色级可编辑 profile；Leg stretch/twist、动画 bake、UI 与 Blender 实现继续留在后续 Maya 稳定阶段之后。

## Maya 切片 61：主轴感知的 Leg IK stretch 与拉伸态匹配

本机 MEL 清单没有给出可独立迁移的 Leg stretch 过程，相关行为仍埋在大型 Body 构建入口中，因此本切片继续采用 clean-room 语义合同。原有 Arm stretch 的数据结构与算法被下沉为 `BodyLimbStretchPlan`：每段根据父关节世界轴投影选择唯一的本地主轴，记录具体 `translateX/Y/Z` 目标，而不再假定所有 limb 都沿 X。Arm 包装保持现有公共字段和节点命名；Leg 包装只选择 Hip→Knee→Ankle 两段及同侧 Ankle IK control，Toes/Foot 不进入腿长计算。

Leg Settings 新增正值 `legGlobalScale` 与左右独立 `legStretch_R/L`。每侧由固定 Hip 起点、Ankle control 测距、缩放后绑定长度、距离比例、no-compression clamp、强度 blend 和双段乘法节点构成。短目标保持原段长，超出绑定长度时两段同比伸长；Foot pivot 位于 Ankle control 下游，因此 `footRoll` 仍移动 Handle，却不会改变 stretch 测距。全部属性、7 个节点名、类型、数值、来源和输出通道进入零修改碰撞预检、构建后快照与审计，并随完整 Leg Rig 的同一个 Undo Chunk 回滚。

`BodyLegIkToFkPlan` 同时解除固定链长限制：它从当前 Body 的 Hip–Knee、Knee–Ankle 世界位置与父关节主轴计算有符号段长，把对应 FK driver translate 加入可写集合，在对齐四层 FK control 后先写入两段长度再切换 blend；后检同时比较 FK 段值和 Hip/Knee/Ankle/Toes 位置、世界轴。这样伸长的 IK 姿态不会在切回 FK 时恢复绑定长度。

首次隐藏进程 PID 38780 的全部 stretch、匹配、Undo 与清理业务检查均通过，但烟雾脚本把纯内存样例的 Z 轴错误当成 Maya 固定轴，因此报告被判失败。改为核对计划所选主轴与真实 DG 输出连线后，Maya 2024 隐藏进程 PID 7720 验证通过：短目标不压缩，1.35 倍目标同比伸长并到达 Ankle control，强度 0/0.5、左右隔离、全局比例补偿、Foot roll 解耦、1.25 倍拉伸态 IK→FK 段长和姿态保持、单次匹配 Undo、完整 Rig Undo 与清理全部成立；两次进程都自行退出。当前未包含 stretch bias、knee pin、squash、Leg twist/volume、角色总控缩放接线、动画 bake、UI 或 Blender 实现。

## Maya 切片 62：主轴无关的通用 Limb twist 与 Leg 集成

本机 MEL 清单只暴露通用 twist 设置和相关大型入口，没有可独立迁移的 Leg twist 过程边界；因此本切片不读取或翻译原脚本行，而是继续按可观察绑定语义 clean-room 实现。原 `BodyArmTwistPlan` 被下沉为 DCC 无关的 `BodyLimbTwistPlan`：Hip→Knee、Knee→Ankle 等每一段都将端点位移投影到起点关节的三个世界轴，只有与单一 X/Y/Z 主轴明确对齐时才允许建图。四元数投影按所选分量与 W 归一化，不再把任意 limb 写死为 X；Arm 兼容包装继续保留既有名称和 X 轴辅助函数，Leg 包装使用 `UpperLeg/LowerLeg` 业务命名。

Maya 适配器复用同一套段基座、`composeMatrix/decomposeMatrix/quatToEuler` 投影和角度分配实现，动态连接 `outputQuatX/Y/Z`、`outputRotateX/Y/Z` 与 helper 对应旋转轴。每侧 Upper/Lower Leg 默认各有两个 helper：基座跟随段起点，pointConstraint 按 1/3、2/3 插值两端位置，轴向角按同一比例分配，另两轴必须保持零。计划、运行快照与后检显式记录主轴、全部来源、约束权重和 swing 泄漏；`BuildBodyLegRig` 把运行依赖预检、全量名称碰撞、twist 创建和审计纳入原有单一事务，Foot 之前的任何 twist 失败都会回滚 mechanisms、controls、blend、IK、visibility 与 stretch。

179 项纯 Python 测试通过，覆盖任意主轴投影、Arm 兼容入口、Leg 完整构建、运行依赖阻断、名称冲突和故障回滚。首次隐藏 Maya 进程 PID 13372 证明节点结构、拉伸跟随、选择保持、单次 Undo、Body 恢复和清理均成立，但验证脚本错误地把 Foot 层级下 IK Ankle 控制空间的旋转直接当作 Body Ankle 局部轴，因此角度断言失败。改用与关节帧对齐的 FK Ankle 控制后，Maya 2024 隐藏进程 PID 29852 验证通过：实际主轴识别为 X，72° twist 精确分配为 24°/48°，纯 Y swing 的轴向输出与全部 helper 旋转为零，1.25 倍拉伸后所有 helper 仍按实时端点插值；一次 Undo 移除完整 Leg/Foot Rig，Body/Fit 和选择恢复，清理后无 Maya/mayapy 残留。当前未包含 Leg volume、twist bias、膝部独立权重、动画 bake、UI 或 Blender 实现；下一切片继续 Maya 的 Leg volume，第二阶段 Blender 仍保持冻结。

## Maya 切片 63：主轴正交的通用 Limb volume 与 Leg 集成

本机 MEL 清单只有 squash/volume 相关的大型入口，没有可单独迁移的 Leg volume 过程，因此本切片继续使用 clean-room 绑定语义。原 `BodyArmVolumePlan` 下沉为 `BodyLimbVolumePlan`：每侧消费已经审计的最终 stretch ratio、同侧 IK/FK 模式和 twist helper 规格；有效长度比例 `r` 仍转换为 `r^-0.5`，使 helper 长度与横截面积乘积近似保持。FK 模式把有效比例固定为 1，IK 模式才读取 stretch；`armVolume_R/L` 或 `legVolume_R/L` 在 0–1 内混合基准比例与守恒比例。

volume 不再固定连接 `scaleY/scaleZ`。通用计划从每个 twist helper 的明确 X/Y/Z 主轴推导另外两条正交轴，并逐 helper 记录轴集合；运行快照同时记录两条实际输入来源，后检要求轴集合、节点类型、模式输入、幂运算、强度输入和全部缩放连接完全一致。Arm 包装保持既有公共类型和节点命名；Leg 使用 `AdvPy_LegVolumeMode/Power/Blend_R/L`。Maya 适配器复用同一构建与捕获路径，`BuildBodyLegRig` 在 twist 成功后、Foot 之前创建 volume，并把六个节点名纳入统一碰撞预检；volume 后检失败会由完整 Leg Rig 的同一 Undo Chunk 回滚此前所有阶段。

181 项纯 Python 测试通过，新增覆盖合成 Z 主轴时只连接 X/Y、Leg volume 节点碰撞和故障整体回滚，同时保留 Arm X 主轴对应 Y/Z 的兼容检查。Maya 2024 隐藏独立进程 PID 16608 验证通过：真实 Leg 主轴为 X，四个右侧 helper 均只在 Y/Z 接收 volume；1.44 倍 IK stretch 得到 `0.833333`，50% 强度得到 `0.916667`，轴向 scale 保持 1。FK 模式、左腿和 `footRoll=70°` 均不会改变右侧 volume 输出；选择保持，一次 Undo 移除完整 Leg/Foot Rig 并恢复 Body/Fit，清理后无 Maya/mayapy 残留。本切片不包含 stretch bias、knee pin、非均匀角色总控缩放、肌肉/网格体积测量、动画 bake、UI 或 Blender 实现；下一切片继续在 Maya 内评估 Leg stretch bias 与 knee pin 的最小可验证合同。

## Maya 切片 64：保持总长的 Leg stretch bias 分配

本机 MEL 清单没有暴露可独立迁移的 stretch bias 或 knee pin 过程边界，因此本切片不翻译原脚本行，继续采用 clean-room 语义合同。`legStretchBias_R/L` 是 0–1 的“上腿额外长度占比”：0 把全部超出绑定长度的增量交给下腿，1 全部交给上腿，默认值按上腿绑定长度占总绑定长度的比例计算，从而与原有两段同比伸长完全一致。纯函数同时要求两段非零、输入有限、有效比例不小于 1，并保证两段绝对长度之和始终等于 stretch 计算出的目标总长。

`BodyLegStretchBiasPlan` 每侧记录原 stretch ratio、两段绑定长度、原 factor 来源、最终 factor 输出与 Knee/Ankle driver 的明确目标 plug。Maya adapter 为每侧建立 delta、candidate、weight 和 factor 四个 utility 节点，只改接原 `LegStretchSegments` 的两个 factor 输入；计划阶段预检属性、节点名、类型、来源和目标可写性，提交后完整采集节点数值与连线。`BuildBodyLegRig` 在原 stretch 后创建 bias，随后重新审计 stretch 的最终来源，再继续 twist、volume 和 Foot；任何属性、接线或后检异常都由同一个 Undo Chunk 撤销整个 Leg Rig。volume 继续只消费总 stretch ratio，不会因段间分配改变横向体积强度。

186 项纯 Python 测试通过，新增覆盖默认值兼容、0/1 极值、总长度守恒、无效输入、统一名称碰撞和末阶段故障回滚。Maya 2024 隐藏独立进程 PID 36084 验证通过：1.4 倍目标下默认 0.5 保持两段同比伸长，bias 1/0 分别得到 7.2+4.0 与 4.0+7.2，0.75 得到 6.4+4.8；所有分配都到达目标且保持总长，volume 输出、左腿隔离和 twist helper 跟随正确。偏置拉伸态 IK→FK 能传递实际段长并保持姿态，选择不变；第一次 Undo 恢复匹配前 IK 状态，第二次 Undo 移除完整 Leg/Foot Rig，进程自行退出且清理无残留。knee pin 需要独立的逐段目标测距与求解拓扑，未与本切片混合；UI、动画 bake 与 Blender 实现继续留在 Maya 核心稳定之后。

## Maya 切片 65：Pole Vector 实时双段测距的 Leg knee pin

本机 MEL 清单没有可独立识别的 knee pin 过程，因此本切片继续使用 clean-room 语义合同，不读取或翻译原脚本表达。`legKneePin_R/L` 取值 0–1：0 完整保留已有 stretch 与 stretch bias 输出；1 时，上腿长度来自 Hip stretch 起点到 Pole Vector control 的世界距离，下腿长度来自 Pole Vector 到 Ankle IK control 的世界距离；中间值在线性混合两组段长。两段实时距离先除以正值 `legGlobalScale` 转回 mechanism 局部长度，并用 `0.0001` 的最小值避免控制重合时把 RP IK 段退化为零。纯 Python `knee_pin_factors` 同时给出最终两段比例和总长度比例，显式覆盖负主轴段长、全局缩放和退化输入。

`BodyLegKneePinPlan` 每侧声明两个 `distanceBetween`、全局比例除法、最小长度 clamp、pin factor、normal/pin blend、总比例加权项和求和节点，共 8 个节点。Maya adapter 在改接 `LegStretchSegments.input2X/Y` 前核对设置节点、全部输入 plug、原 stretch bias 来源和名称冲突；提交后逐项采集节点类型、矩阵来源、运算、常量和最终目的连接。完整 `BuildBodyLegRig` 在 stretch bias 后创建并审计 knee pin，再重新审计 bias 与基础 stretch 的最终来源；twist 继续跟随真实 Body 端点，volume 改为消费 knee pin 算出的最终总长度比例。任一 pin、后续 volume 或 Foot 复检失败都由原有单一 Undo Chunk 撤销整个 Leg Rig。

191 项纯 Python 测试通过，新增覆盖零权重兼容、全权重双段距离、全局比例补偿、退化最小长度、无效输入、统一名称碰撞和故障整体回滚。Maya 2024 隐藏独立进程 PID 28152 验证通过：1.25 倍 Ankle 目标和 0.75 stretch bias 下，pin 0 保持 5.5/4.5 段长；pin 1 使用 7.211102/8.485281 的 Hip→PV、PV→Ankle 距离，使 Knee 到达 Pole Vector 且 Ankle 保持目标；pin 0.5 连续得到 6.355551/6.492640，图中总比例与纯函数一致并驱动 volume。左右隔离、twist helper 跟随、pin 态 IK→FK 姿态及段长传递、选择保持和两层 Undo 均通过，验证进程自行退出且无残留。当前 volume 按最终总长统一缩放每段 helper，尚未提供上、下腿独立体积比例；角色总控缩放接线、动画 bake、UI 与 Blender 实现仍留在后续 Maya 阶段。

## Maya 切片 66：单事务 Character Rig 与统一 Global control

本机 MEL 清单没有可独立识别的完整角色总控过程，因此本切片不读取或翻译原脚本表达，继续用 clean-room 可观察语义建立合同。此前 `BuildBodyArmRig` 与 `BuildBodyLegRig` 各自拥有事务，适合单独构建，但不能保证完整角色要么全部成功、要么全部恢复。新的 `BuildBodyCharacterRig` 先从同一份干净 Body/Fit 安全快照生成两套计划，确认输入一致后再进入唯一外层 Undo Chunk；Arm/Leg 内部构建路径可复用预计算计划并关闭嵌套事务，但原有独立公共入口与行为不变。

DCC 无关的 `BodyCharacterGlobalPlan` 明确声明 `AdvPy_CharacterControls → AdvPy_GlobalOffset → AdvPy_Global` 层级、地面圆环方向、正值 `globalScale`、九个受控根及两个 stretch 比例目标。Maya adapter 在任何写入前再次要求这些根都位于世界根级、TRS 中性、可写且没有输入连接，并核对比例属性存在、值为 1、可写且未连接。提交时 Global 的 translate/rotate 直接驱动 Body 根和 Arm/Leg 各四个顶层根，`globalScale` 驱动九个根的 scaleXYZ，并同时连接 `armGlobalScale` 与 `legGlobalScale`。这种直接接线保留既有绝对 DAG 路径，避免重挂父级破坏已经审计的 limb 合同；构建后再次复检层级、曲线、全部来源、Body 绑定姿态和 Fit 不变性，任何偏差都由同一事务回滚。

197 项纯 Python 测试通过，新增覆盖统一计划、名称碰撞在事务前阻断，以及 Global 末阶段失败时 Arm/Leg 整体回滚。Maya 2024 隐藏独立进程 PID 25500 验证通过：Global 平移、旋转和 1.5 倍等比缩放正确驱动九个根，Arm/Leg 的 1.25 倍伸长在全局缩放下保持局部段长比例并到达目标，Leg 满权重 knee pin 的两段世界距离除以 1.5 后正确进入 mechanism；选择保持，一个 Undo 移除完整 Character/Arm/Leg Rig 并恢复 Body 绑定姿态与 ReBuild 安全，清理后无残留节点或进程。当前只支持单一正值等比缩放，不支持非均匀缩放、shear、多总控空间或 World Match；下一切片继续 Maya 的 Hand/手指最小合同，Blender 第二阶段仍冻结。

## Maya 切片 67：五指 Fit 源与镜像 Body 骨架

本机 MEL 清单只出现一个 Finger Fit 辅助过程，没有可独立识别的手部构建边界，因此本切片不读取或翻译原脚本表达。DCC 无关的 `synthetic_body_with_hand_source_fit_template` 复用现有 18 关节全身源，在 Right Wrist 下增加 Thumb、Index、Middle、Ring、Pinky 五个四关节链；统一导出 `BodyHandDigit`、稳定段名和源关节名，作为后续手指控制与保存加载的共同语义。五指在 Y-Up 时分布于 XZ 手掌平面、Z-Up 时分布于 XY 平面，所有长度和间距只来自仓库内的确定性合成参数。

`BuildSyntheticBodyWithHandSourceFit` 复用通用 Fit 模板事务：写入前核对空容器、完整设置和全部名称，使用 Middle1 作为 Wrist 的显式朝向子级，再创建 38 个源 joints、计算 28 个朝向并复检世界位置、标签和容器输入。通用 symmetry 与 Body builder 不需要 Maya 专用分支即可把 20 个 Right 手指源 joints 镜像成左右 40 个 Body joints，最终得到 6 个中心、32 个右侧和 32 个左侧 joints；这验证了 Hand 合同仍位于核心层，Maya adapter 只负责既有 joint、标签、朝向和事务操作。

201 项纯 Python 测试通过，新增覆盖五指完整父链、Finger 标签、Y/Z Up 手掌平面、Wrist 明确分支和应用层单事务。首次隐藏 Maya 2024 进程 PID 13652 中，五指拓扑、70 关节 Body、镜像朝向、重复构建、Undo 与清理均通过，但烟雾脚本用浮点完全相等比较朝向补偿前后位置，因此结果被判失败。改用与核心合同一致的 `1e-5` 容差并记录漂移后，PID 23284 验证通过，最大位置漂移为 `8.88e-16`；Body 和 Fit 各自一次 Undo，无进程或场景残留。本切片不创建手指 controls、curl/spread 属性、手掌变形、Skin 权重或 UI；下一切片继续 Maya 的分层手指 FK controls，Blender 第二阶段保持冻结。

## Maya 切片 68：Wrist 跟随的双手分层 Finger FK controls

本机 MEL 清单没有新增可隔离的 Hand control 构建过程，因此继续从上一切片的五指 Body 语义 clean-room 设计。`BodyHandFkControlPlan` 要求双侧 Wrist 以及五根完整的 1→2→3→End Body 父链和 side 一致；每侧在 Wrist 下声明一个 `AdvPy_HandFKControls_R/L` 根，每指前三节建立逐级嵌套的 offset/control/constraint。下一层 offset 以当前 Body 指节世界帧对齐后挂在上一层 control 下，使控制层和受驱动骨链共享相同的父级旋转传播，Wrist 无论由 Arm FK 或 IK 驱动，手指控件都能随之运动。控制半径按 1、2、3 节递减，末端 End joint 不生成动画 control。

应用层在 Body ReBuild 安全审计之外增加 `BodyHandFkInputSnapshot`，显式采集 30 个目标的 rotate 可写轴与输入来源。任何锁定或既有连接、节点名冲突、五指缺失或父链错误都会在事务前停止；进入事务后再次核对 Body/Fit 和输入快照，再创建两个 Wrist 子根、30 个 NURBS controls 与 30 个 orientConstraints。后检复用通用 FK control 状态，并额外验证 Hand 根的 Wrist 父级、本地中性通道和世界帧；Body 绑定姿态或 Fit 发生变化时撤销整套控制。

206 项纯 Python 测试通过，新增覆盖双侧五指计划、逐级控制父链、递减半径、锁定/已连接输入、名称冲突与后检故障回滚。Maya 2024 隐藏独立进程 PID 33588 一次验证通过：2 个 Wrist 子根、30 个曲线控制及约束均正确，右 Index 前两层旋转能移动真实 Body 指尖，第二、三层 control 继续贴合对应指节，左手不受影响；归零恢复姿态，选择保持。控制存在时 ReBuild 因依赖明确阻止，一个 Undo 移除全部 Hand controls 后 70 关节 Body 与 Fit 保留且 ReBuild 再次安全，清理后无场景或进程残留。本切片尚未把 Hand 纳入 Character 顶层事务，也不包含 curl/spread 聚合属性、SDK、手掌变形、Skin 权重、UI 或 Blender 实现。

## Maya 切片 69：Character 顶层事务中的可选 Hand FK

`BuildBodyCharacterRig` 继续以 30 关节 Body 作为兼容输入，不要求旧角色具备手指；如果 Body 中没有任何稳定五指名称，计划仍只包含 Arm、Leg 和 Global。检测到任意五指关节时，应用层要求 Thumb/Index/Middle/Ring/Pinky 的双侧 1→2→3→End 共 40 个关节全部存在，残缺集合会形成明确 blocker 并在事务前停止，不能静默降级。完整集合复用同一份 Body/Fit 安全快照生成 `BodyHandFkBuildPlan`，避免第三次宿主读取造成计划漂移。

Hand 独立入口保留原有事务和执行前 Body/Fit 复检；内部 `_apply_plan` 新增由 Character 调用的无嵌套事务模式。Arm 与 Leg 创建后会给 Body limb 写入约束，因此 Character 内不再重复要求整棵 Body 处于 ReBuild-safe 状态，但仍会重新采集 30 个手指 rotate 输入并与预演快照比较，再创建 Hand。构建顺序为 Arm→Leg→Hand→Global；Hand roots 直接位于 Wrist_R/L 下，随 Body Wrist 继承 Global 和 Arm FK/IK 结果，不增加 Global 的九个根级驱动目标。Hand 后检或后续 Global 后检失败时，外层 Undo Chunk 会恢复此前全部阶段。

209 项纯 Python 测试通过，新增覆盖 30 关节兼容、70 关节自动 Hand、残缺五指预检和 Hand 后检失败时 Arm/Leg 整体回滚。Maya 2024 隐藏独立进程 PID 28336 一次验证通过：70 关节 Character 同时建立 12 个 Arm mechanisms、20 个 Leg mechanisms、2 个 Hand roots、30 个 Hand controls 和统一 Global；右 Wrist 分别由 Arm FK 与 IK 驱动时，Hand 根世界帧和 Index control 都正确跟随，左手保持不变。恢复中性姿态后，一个 Undo 移除完整 Arm/Leg/Hand/Global Rig，同时保留 70 关节 Body、Fit、选择与 ReBuild 安全；进程自行退出且启动前后均无 Maya/mayapy 残留。本切片不增加 curl/spread、SDK、手掌变形、自动手指权重、UI 或 Blender 实现。

## Maya 切片 70：可叠加的 Hand curl/spread 聚合姿态

本机 MEL 清单没有可隔离的手指姿态构建过程；名称含 Curl 的另一条记录只接受脚本路径并返回字符串，不构成 Hand 语义来源。本切片从现有五指 FK 合同独立定义聚合层：每侧 Hand root 添加 `handCurl`、Thumb/Index/Middle/Ring/Pinky 五个单指 curl 和 `handSpread`，共 14 个可动画属性。curl 使用角度值，`handCurl` 范围为 -90° 到 90°，单指范围为 -45° 到 45°；每指 1/2/3 节按 0.45/0.75/1.0 权重相加。spread 范围为 -30° 到 30°，只作用第一节；Middle 保持中心，Thumb/Index/Ring/Pinky 使用 1/0.5/-0.5/-1，并在 Left 侧反号，以产生左右对应的扇开行为。

聚合值不能直接覆盖 FK control.rotate，否则动画师无法继续逐节修形；也不能直接覆盖既有 offset.rotate，因为 offset 的非零本地旋转保存相邻指节的绑定朝向。因此每个 offset 与 control 之间新增一个零 TRS 的 Pose transform，共 30 层；30 个 `blendWeighted` 只连接 Pose.rotateZ，8 个 `multDoubleLinear` 只连接 Pose.rotateY。通用 limb FK 规格新增可选 control parent，Arm/Leg 默认行为和路径保持不变。核心快照与 Maya adapter 复检 Pose 父链、中性通道、属性范围、keyable 状态、节点类型、输入、权重和最终输出来源；任一异常继续由 Hand 或 Character 的原事务整体回滚。

212 项纯 Python 测试通过，新增覆盖 Pose 计划计数、curl 轴与权重、spread 左右符号、错误输出审计和 Pose 后检失败回滚。首次隐藏 Maya 2024 进程 PID 8176 按设计在后检中发现直接连接旧 offset 会覆盖绑定朝向，Character 未提交且进程自行退出。改为独立 Pose 层后，纠正性隐藏进程 PID 47064 验证通过：右侧 `handCurl=30` 得到 13.5°/22.5°/30°，再加 `indexCurl=10` 得到 18°/30°/40°；Index1 control.rotateZ 仍无输入并可额外叠加 12°。`handSpread=20` 产生 Thumb/Index/Ring/Pinky 的 20°/10°/-10°/-20°，左手不受右侧操作影响。归零恢复绑定姿态，一个 Undo 移除 Character 与全部 Pose/DG 节点，70 关节 Body、Fit、选择和 ReBuild 安全保留，启动前后无 Maya/mayapy 残留。本切片不包含手势预设、逐指 spread、拇指专用 opposition、自动手指权重、UI 或 Blender 实现。

## Maya 切片 71：DCC 无关的 Hand Pose 文档保存与恢复

本切片没有继续增加手指节点，而是把已经稳定的 Hand 动画入口收束成保存/加载合同。纯 Python `BodyHandPoseDocument` 使用 schema v1 保存双侧 14 个聚合属性，以及 Thumb/Index/Middle/Ring/Pinky 各 1/2/3 节共 30 个 FK control 的本地三轴旋转。文件只包含 `R/L`、digit、segment 和数值，不保存 Maya DAG 路径、节点类型或宿主命令；固定顺序和 SHA-256 内容摘要使损坏、手改漏项、重复语义键和非有限数值在接触场景前被拒绝。这是未来第二阶段可交给 Blender adapter 消费的稳定数据边界，但本切片不实现 Blender。

`InspectBodyHandPoseRig` 要求本工程拥有的 70 关节 Body，并在任意当前姿态下复检 Hand FK 父链、Pose 层、curl/spread DG 网络和全部语义通道。导出先完成场景只读检查，再以同目录临时文件写入、解析复检和原子替换落盘，目标已存在时拒绝覆盖。导入先解析并验证文件，再检查聚合值范围、属性可写性、FK 三轴可写性与输入连接；执行前和事务开始后各复检一次场景快照，只对实际差异开启一个 Undo Chunk，写入后重新生成语义文档与目标逐项相等，否则由宿主事务回滚。已经相同的姿态直接返回零修改结果，不制造空 Undo。

217 项纯 Python 测试通过，新增覆盖动画姿态下的结构审计、语义 JSON 往返与摘要防篡改、原子导出拒绝覆盖、导入幂等，以及不安全通道在事务前阻断。Maya 2024 隐藏独立进程 PID 4704 一次验证通过：包含右手聚合 curl、左手 spread 和双侧手动 FK 旋转的姿态被导出为 14+30 条语义记录，JSON 不含 Maya 路径；修改五个语义通道后，导入精确恢复文档与两侧真实指尖位置，重复导入为零修改，一次 Undo 恢复导入前姿态。随后一次 Undo 移除完整 Character，70 关节 Body、Fit、选择、ReBuild 安全和临时目录清理全部通过，进程自行退出且无 Maya/mayapy 残留。本切片不包含命名手势库、缩略图、UI、动画曲线/keyframe 写入、namespace 重映射或 Blender adapter。

## Maya 切片 72：显式角色根驱动的 Hand Pose namespace 映射

本机 MEL 清单把 Poser 与 namespace 解析列为独立过程，但本切片不读取或翻译其实现。制作场景经常把同一角色文件引用为多个 Maya namespace；schema v1 已经没有场景路径，剩余缺口是把一组语义通道确定性解析到调用方指定的角色实例。新入口继续使用 `body_root_name`，例如 `RigA:Root_M` 或嵌套的 `Shot:RigA:Root_M`。应用层从实际 Body 根提取 namespace，要求 70 个 Body joints 全部属于同一个 namespace，再把 Hand roots、30 个 offset/Pose/control、30 个约束和 38 个 curl/spread DG 节点规划到相同前缀。没有显式根时仍只接受唯一的无 namespace `Root_M`，不会读取选择、扫描所有 namespace 或猜测角色。

核心 Hand 计划新增可选 namespace 命名前缀，五指、侧向和节段的语义键保持不变；Pose 文档生成和摘要因此不会包含 `RigA`、`RigB` 或任何 Maya 路径。Maya adapter 在捕获通道时只剥离节点 namespace 来匹配语义控制名，实际写入仍使用完整 DAG 路径。结构预检会同时核对 namespaced 父链、约束源、DG 节点和目标 plug，避免同场景另一实例的同名 control 被短名解析命中。导入继续执行文件复检、场景复检、单事务写入和结果文档复检。

218 项纯 Python 测试通过，新增覆盖嵌套 namespace 的完整 Hand/Pose 计划、通道审计，以及生成文档不携带实例前缀。Maya 2024 隐藏独立进程 PID 32036 一次验证通过：同一份自行生成的 70 关节角色资产被引用为 `RigA` 与 `RigB`，从 A 导出的 14+30 Hand Pose 文档不含 namespace；修改 B 的五个语义通道后导入，B 的两侧真实指尖与 A 对齐，而 A 在导入和 Undo 前后保持不变。重复导入为零修改，一次 Undo 只恢复 B 的导入前姿态，选择、引用场景清理和临时 `.ma`/JSON 删除均通过，进程自行退出且无 Maya/mayapy 残留。本切片不增加自动角色发现、namespace 菜单、命名预设库、UI、动画曲线写入或 Blender adapter。

## Maya 切片 73：Hand Pose 当前帧原生动画写键

本机 MEL 清单含有按键、线性按键和 Poser 相关过程名，但本切片只使用过程名做能力分区，没有读取、翻译或派生其实现。核心 Hand Pose 通道合同新增只读、静态写入、当前帧写键三种访问模式：只读允许从动画或其他驱动结果生成语义文档；静态写入继续要求通道没有输入连接；写键模式只接受未连接且可 key 的通道，或唯一输入为 Maya 原生 `animCurve*` 的通道。约束、表达式、多输入及未知 DG 驱动不会被断开或覆盖，而是在事务开始前明确拒绝。

`ImportBodyHandPose(..., keyframe=True)` 继续复用 schema v1、显式角色根、文件摘要和语义差异计划；它在一个 Undo Chunk 内只给变化的聚合属性与 FK control 三轴写入当前帧 key，然后强制 Maya 在不改变当前时间的前提下重新求值，再读取完整 Hand 结构与语义文档做事务后复检。重复导入不增加 key 或空事务。默认 `keyframe=False` 的静态行为不变，避免已有调用方在不知情时改写动画曲线。

220 项纯 Python 测试通过，新增覆盖原生动画源在静态/写键模式下的不同准入规则，以及非动画驱动在事务前阻断。首次隐藏 Maya 运行如预期暴露了 `setKeyframe(value=...)` 在未改变当前时间时延迟求值的问题，事务后复检拒绝结果并回滚，未生成通过报告；将当前帧显式重新求值纳入 adapter 后，Maya 2024 隐藏独立进程 PID 16500 验证通过。自生成 70 关节角色的 3 个聚合属性和 2 个 FK controls 共 9 个 plug 先建立第 1/20 帧原生动画，在第 10 帧从五个语义差异写入 9 个 key；双侧真实指尖和文档恢复目标姿态，重复导入为零修改，一次 Undo 删除第 10 帧 key 并恢复原插值姿态。只读动画导出、静态导入拒绝动画源、未知 `multiplyDivide` 驱动拒绝、选择保持、临时文件与场景清理均通过，进程自行退出且无 Maya/mayapy 残留。本切片不增加区间 bake、切线策略、动画片段、自动角色发现、UI 或 Blender adapter。

## Maya 切片 74：显式单侧 Hand Pose 导入

一份 Hand Pose 文件继续保存完整双手，避免引入左右侧不同 schema 或无法独立校验的残缺文档。`ImportBodyHandPose` 新增可选 `target_side`：`None` 保持双手导入，`FitBuildSide.RIGHT/LEFT` 只恢复明确目标侧，Middle、字符串和其他值在修改场景前拒绝。核心 `merge_body_hand_pose_document` 把目标侧取自输入文件、非目标侧取自当前场景，重新生成带摘要的完整预期文档；差异计划、事务后结果和重复执行都针对该文档，而不是通过跳过全量复检实现局部写入。

通道审计仍要求两侧 14 个聚合属性、30 个 FK controls 及其结构完整，但静态/写键可写性只约束目标侧。因此非目标侧可以保留动画、约束、表达式或未知 DG 驱动；选择双手或选择该被驱动侧时，同一驱动仍会在事务前阻断。Maya adapter 不需要新增场景猜测或选择依赖，只执行已过滤的语义变化集合，现有静态写入、当前帧写键、namespace 与 Undo 边界保持不变。

221 项纯 Python 测试通过，新增覆盖完整文件到右手目标的语义合并、非目标侧未知驱动放行、错误目标侧拒绝、幂等和单事务。首次 Maya 2024 隐藏进程 PID 51980 的全部业务检查均通过，但验证脚本把 `multiplyDivide` 到角度属性之间由 Maya 自动插入的 `unitConversion` 错当成连接被改写，因此报告判定失败；改为比较导入前后的实际 source plug 后，隐藏独立进程 PID 14968 验证通过。左手聚合属性和 FK 三轴保持外部 DG 驱动时，双手与左手导入均在事务前停止，右手两个聚合值和一个 FK control 共三个语义变化独立恢复；左手指尖与连接保持不变，结果等于合并后的完整预期文档，重复执行零修改，一次 Undo 恢复导入前完整姿态。选择、临时文件、场景清理和进程退出均通过。本切片不增加姿态镜像、命名手势库、区间 bake、UI 或 Blender adapter。

## Maya 切片 75：行为坐标系中的 Hand Pose 左右镜像

本机清单只有通用 Mirror、Poser 和动画曲线翻转过程名，没有可证明的 Hand Pose 轴映射边界，本切片没有读取或翻译其实现。镜像规则来自本工程已经验证的 Body 行为朝向：Left 局部帧等于世界 X 反射后的 Right X/Y 轴，并重新构造右手 Z 轴。由局部旋转矩阵共轭可得，左右聚合属性保持相同语义值，FK Euler 在共享旋转顺序下按 `(-X, -Y, Z)` 转换。核心 `mirror_body_hand_pose_document` 保留源侧，替换对侧同名聚合值与同指同节旋转，并重新生成稳定摘要；零值取反后规范为 `0.0`，避免 `-0.0` 造成数值相等但 JSON 摘要不同。

`MirrorBodyHandPose` 从当前场景完整语义文档生成镜像预期结果，只要求目标侧满足静态或当前帧写键条件。源侧可以保留动画、约束或未知 DG 驱动，但目标侧不安全输入会在事务前拒绝。执行前和事务开始后都复检完整 Rig，单一 Undo 只提交目标侧实际变化，完成后重新读取双手文档与预期结果逐项比较；源侧、namespace、选择和重复执行均不依赖隐式场景状态。

222 项纯 Python 全量测试通过；三轴推导后的针对性测试还暴露并修正了 `-0.0` 摘要稳定性。首次 Maya 2024 隐藏进程 PID 14080 验证了语义镜像、源侧驱动隔离、幂等、Undo、目标侧拒绝和清理，但同号 FK 假设未通过五指几何镜像检查，因此报告判定失败。按行为帧矩阵推导改为 `(-X, -Y, Z)` 后，隐藏独立进程 PID 52184 验证通过：右手外部驱动的 `handCurl`、`handSpread` 和五个第一节 FK 形成七个语义变化，镜像后 Thumb/Index/Middle/Ring/Pinky 五个末端全部关于世界 X=0 对称，最大位置误差为 `1.11e-16`。源手姿态与驱动未改变，重复镜像零修改，一次 Undo 恢复左手原姿态，目标侧未知驱动在事务前停止，选择、场景与进程清理通过。本切片不增加自定义镜像平面、不同旋转顺序、命名手势库、区间 bake、UI 或 Blender adapter。

## Maya 切片 76：安全中文名称的 Hand Pose 预设库

本机清单只用于确认存在 Poser 新建、保存和浏览等能力名称，没有读取相关过程体、文件布局或 UI 实现。本切片把上一阶段稳定的 Hand Pose schema 与导入导出用例组合成独立的 clean-room 文件目录合同：一个预设对应一个 `.handpose.json` 普通文件，预设名先做 NFC 规范化，允许中文，但限制为 1–64 个字符，并拒绝首尾空白、路径分隔符、控制/格式字符、结尾句点、Windows 保留名以及大小写或 Unicode 规范化后的冲突。目录扫描不递归、不跟随符号链接，也不把任意 JSON 当作预设；发现冲突或损坏文件时整份只读清单失败，不返回部分可信结果。

`SaveBodyHandPosePreset` 复用已有场景只读检查、同目录临时文件复检、原子替换与拒绝覆盖；`InspectBodyHandPosePresetLibrary` 只读取和校验 schema/摘要；`ApplyBodyHandPosePreset` 允许大小写不敏感查询，但返回磁盘中的规范显示名，并复用既有显式角色根、namespace、静态/当前帧写键、单侧合并、场景漂移检查、幂等与一个 Undo Chunk。预设文件是场景事务之外的持久资产，因此 Maya Undo 只恢复姿态，不删除已经保存的文件；损坏文件会在进入场景事务前带文件名拒绝。

223 项纯 Python 全量测试通过。Maya 2024 隐藏独立进程 PID 50944 一次验证通过：中文预设“握拳Test”原子保存并由只读清单复检，大小写变体拒绝覆盖而查询仍返回规范名称；右手两个语义变化独立恢复，左手保持不变，真实右手指尖回到保存姿态，重复应用零修改。一次 Undo 恢复应用前场景而预设文件继续存在；非法名称和损坏预设均在场景修改前拒绝，选择、临时目录、场景和进程清理通过。本切片不增加预设重命名、删除、缩略图、标签/搜索、网络共享目录、产品 UI 或 Blender adapter；第二阶段仍保持冻结。

## Maya 切片 77：路径无关的 FitSkeleton 文档往返

本机清单只用于确认 FitSkeleton 导入/导出入口的过程名、调用关系和规模，没有读取或翻译过程体。本切片独立定义 DCC 无关的 FitSkeleton schema v1：文档保存场景 Up Axis、已验证轴配置、可移植安全设置、关节短名与父级短名、局部位置、Maya 标签的语义值、行为世界轴及当前核心支持的元数据，并以 SHA-256 摘要锁定内容。Maya DAG 路径、容器路径、场景对象列表以及 pre/post ReBuild 脚本文本不进入文档；若源容器的这些非可移植字段非空，导出会明确拒绝，避免静默丢失数据。

`ExportFitSkeleton` 保持场景只读，限定 `.fit.json` 普通文件、拒绝覆盖，并通过同目录临时文件写入、落盘复检和原子替换生成结果。`ImportFitSkeleton` 只接受调用方明确给出的现有空 FitSkeleton 容器，并要求目标的 Up Axis 与轴配置和文档一致；文件摘要与结构、完整安全设置通道、输入连接、全局名称冲突和目标空状态都在进入事务前检查。实际导入按父级顺序创建 joints，在创建子级前恢复父级的行为世界轴，同时恢复标签、元数据和实际有差异的安全设置；完成后从真实场景重新生成完整文档，并在同一个 Undo Chunk 内做语义复检。任何阶段失败都会回滚，重复导入到非空目标会在事务前停止。

227 项纯 Python 全量测试通过。首次隐藏 Maya 验证因验证脚本在 Undo 后读取已经删除的导入路径而判定失败，没有生成通过报告；修正验证顺序后，Maya 2024 隐藏独立进程 PID 2832 通过。自行生成的 38 关节五指 Fit 完成两项安全设置、全部标签、元数据、局部位置和行为世界轴往返；最大世界位置误差为 `8.88e-16`，最大世界轴误差为 `1.39e-16`。拒绝覆盖、摘要篡改、被驱动设置、重复导入、选择保持、一次 Undo 回到空容器、临时目录和场景清理均通过，进程自行退出且无 Maya/mayapy 残留。

本切片不合并现有 Fit、不自动创建目标容器、不改写不同轴配置，也不迁移场景对象列表、ReBuild 脚本、产品 UI 或 Blender adapter。第二阶段仍保持冻结；未来 Blender 只复用已经稳定的文档语义和核心校验，不反向改变 Maya 行为。

## Maya 切片 78：FitSkeleton 安全增量合并

本机清单只用于确认 FitSkeleton 合并能力的过程名与规模，没有读取、翻译或派生过程体。核心 `plan_fit_skeleton_document_merge` 把当前场景和输入文件都视为 schema v1 语义文档：Up Axis、轴配置和全部可移植设置必须一致；每个共享短名关节的父级、局部位置、行为世界轴、标签和元数据也必须在容差内一致。满足这些条件时，联合文档保留当前关节顺序与内容，只按输入文档的父级优先顺序追加缺失关节。任何共享语义冲突都会进入只读计划问题，不提供自动覆盖、重父级、移动、重朝向或删除策略。

`MergeFitSkeleton` 在事务前解析并复检摘要、读取完整目标文档，只对待新增短名做全场景重名检查。执行前再次生成同一计划以发现文件或场景漂移，然后在一个 Undo Chunk 内逐级创建缺失关节，并在创建子级前恢复父级标签、元数据与行为世界轴；完成后从真实 Maya 场景重建联合文档并全量比较。输入已经是当前文档的子集或完全相同时返回零新增且不打开空事务。文件作为场景事务外的输入资产，Maya Undo 不修改或删除它。

230 项纯 Python 全量测试通过，新增覆盖安全联合、既有关节保持、幂等、共享语义冲突、外部重名与事务后失败回滚。Maya 2024 隐藏独立进程 PID 16696 一次验证通过：从自行生成的 38 关节五指 Fit 删除五条手指根分支形成 18 关节基线，再由原文档只新增 20 个手指关节。18 个既有关节的 DAG 路径和 UUID 全部不变，合并结果与输入文档一致；重复执行不增加 Undo，一次 Undo 只移除 20 个新增关节，共享 Spine 位置冲突在场景修改前拒绝。选择、输入文件、临时目录和场景清理均通过，进程自行退出且无 Maya/mayapy 残留。

本切片不处理冲突覆盖、重父级、删除式同步、设置合并、空容器导入、容器自动创建、产品 UI 或 Blender adapter。第二阶段仍冻结；增量联合规则已经位于 DCC 无关核心，未来宿主只负责安全物化同一计划。

## Maya 切片 79：从文档新建并导入 FitSkeleton

本机清单只用于确认 FitSkeleton 容器创建与文件导入是相邻能力边界，没有读取或翻译过程体。`CreateAndImportFitSkeleton` 把原先需要调用方分两步执行的空容器创建与完整文档导入组合为一个应用用例。计划先解析 `.fit.json` 和摘要，以文档 Up Axis 构造安全根级 `FitContainerSpec`，合成 21 项完整容器设置：文档中的可移植值原样恢复，场景对象列表和 pre/post ReBuild 脚本保持空默认值。容器名与全部关节短名一次做全场景冲突检查，Maya 场景 Up Axis 不一致时在修改前停止。

新容器目前只接受默认 X Primary、Y Secondary 且关闭 World Match 的轴配置。现有 `ImportFitSkeleton` 仍可向调用方预先配置好的空容器导入非默认轴配置；在尚未建立并验证轴配置属性的创建写入合同前，新建路径不会猜测 Maya enum 布局。执行时容器、设置、标签、元数据、局部位置和行为世界轴全部位于一个 Undo Chunk。三条导入路径共用 `_materialize_fit_joints` 的父级优先编排，避免空容器导入、新建导入和增量合并各自维护不同的关节创建顺序。事务后同时审计容器形状、平面、显示半径、锁定通道、完整设置和重新捕获的语义文档；失败会连同新容器整棵回滚，输入文件不属于 Maya Undo。

233 项纯 Python 全量测试通过，新增覆盖空场景成功路径、完整设置、全局重名、非默认轴配置拒绝和事务后失败回滚。Maya 2024 隐藏独立进程 PID 13652 一次验证通过：从自行生成的 38 关节五指文档，在空场景一次创建半径 4.0 的根级容器、21 项设置和完整关节树，并恢复可移植显示设置与 Root 元数据。计划与两条失败路径保持场景只读；一次 Undo 删除容器及全部关节但保留输入文件和用户选择。外部 `Root` 重名及 Y/Z Up 不匹配均在事务前拒绝，临时目录和场景清理通过，进程自行退出且无 Maya/mayapy 残留。

本切片不创建非默认轴配置属性，不处理冲突覆盖、重父级、删除式同步、场景对象列表、ReBuild 脚本、产品 UI 或 Blender adapter。Blender 第二阶段继续冻结；新容器的显示对象和 Undo 生命周期仍由各宿主分别实现。

## Maya 切片 80：新容器非默认轴配置恢复

上一个切片保留的新建导入轴配置缺口已经闭合。`MayaFitJointHost.add_fit_orientation_axis_configuration` 在事务内为本工程刚创建的容器新增 `primaryAxis`、`secondaryAxis` 与 `worldmatch` 三个属性；Primary/Secondary 使用明确的 `X:Y:Z:-X:-Y:-Z` enum 布局，应用层仍只传递 DCC 无关的 `FitOrientationAxisConfiguration`。方法要求三项属性全部不存在，任何同名属性都会拒绝而不是覆盖；属性创建开始前标记事务已修改，因此中途失败也会触发完整 Undo。

`CreateAndImportFitSkeleton` 不再把非默认配置降级或挡在计划阶段。容器与 21 项设置创建后，adapter 按文档写入轴配置，再使用共享的父级优先物化器恢复关节树。既有 `_optional_enum`/`parse_fit_axis_direction` 读取路径负责事务后复检，因此写入的 Maya enum 标签、数值和文档语义必须闭环一致。场景 Up Axis 仍是 Maya 全局状态，不由文件静默改写；不一致时继续在事务前拒绝。

233 项纯 Python 全量测试通过，相关测试把 `Primary=+Y`、`Secondary=+Z` 的非默认配置恢复到新建目标，同时继续覆盖全局重名、Up Axis 不匹配和失败回滚。Maya 2024 隐藏独立进程 PID 37492 一次验证通过：自行生成的 38 关节文档携带 `Primary=-X`、`Secondary=+Z`、`World Match=true`，空场景新建导入后，Maya 实际属性的 enum 布局、索引 `3/2`、bool 值和重新捕获的语义文档全部一致。半径 4.0、21 项设置、选择保持、一次 Undo、重名与 Up Axis 失败路径、临时目录及场景清理继续通过，进程自行退出且无 Maya/mayapy 残留。

本切片不改写场景 Up Axis，不覆盖已有轴属性，也不增加冲突覆盖、重父级、删除式同步、场景对象列表、ReBuild 脚本、产品 UI 或 Blender adapter。六方向枚举是本 Python 框架明确维护的 Maya adapter 合同，不声称复制授权 MEL 的内部 enum 排列。

## Maya 切片 81：游戏导出 Root Motion 驱动

本机清单只用于确认游戏导出与 Root Motion 是相邻能力边界，没有读取、翻译或派生相应过程体。核心 `BodyRootMotionPlan` 独立定义一个窄而稳定的导出语义：输出 joint 位于场景根层，只传递 Body root 的平面位移和 Up 轴旋转。Y-Up 映射 X/Z 位移与 Y 旋转，Z-Up 映射 X/Y 位移与 Z 旋转；垂直位移和两个倾斜旋转不进入输出。显式 namespace 角色会在自己的 namespace 内生成输出和约束名称，因此不依赖当前选择或自动角色猜测。

`BuildBodyRootMotion` 只接受带有本工程 provenance、声明 Fit 来源与实际关节数量一致的 Body。输出 joint 和两个约束名称在事务前全场景预检，事务开始后再次确认 Body 快照和名称没有漂移；Maya adapter 使用分别跳过垂直轴和非 Up 旋转轴的原生 point/orient constraint，并在创建后从真实 incoming plugs、constraint targets、层级和中性属性重建快照审计。Body 不会被重父级、改名或接入新的输入，一个 Undo Chunk 只负责 Root Motion 输出与约束，用户选择保持。

239 项纯 Python 全量测试通过。Maya 2024 隐藏独立进程 PID 31372 使用自行创建并带独立 provenance 的最小 Body，一次覆盖 Y-Up 和 Z-Up：三轴位移与旋转输入分别得到 X/Z+Y yaw 和 X/Y+Z yaw 输出，过滤轴保持零，结构审计、Body 层级、选择、一次 Undo、Fit/Body 保留及场景清理全部通过，进程自行退出且无 Maya/mayapy 残留。

本切片不执行 FBX 导出，不创建完整导出骨架，不重父级或复制 Body，不烘焙动画，也不定义游戏引擎坐标转换。它是 Maya 第一阶段的导出前置能力；后续先在 Maya 中补齐 bake、导出集合和文件写入边界，Blender 第二阶段继续冻结。

## Maya 切片 82：Root Motion 动画 bake

本切片继续沿上一阶段的显式 Root Motion 合同工作，不读取清单中通用动画 bake 或导出过程的实现。核心 `BodyRootMotionBakePlan` 接受调用方明确提供的整数起止帧和正整数采样步长，要求结束帧落在采样序列上，并生成包含首尾的确定性帧集合。采样结果保存完整 TR，但审计继续要求垂直位移和两个倾斜轴为零；最终只物化两个平面 translate 通道和一个 Up 轴 rotate 通道。

`BakeBodyRootMotion` 要求输入仍是上一切片验证过的实时 point/orient constraint 状态，并再次确认 owned Body provenance、Body 快照和实时驱动没有漂移。Maya adapter 在同一个 Undo Chunk 内逐帧读取输出并恢复原 currentTime，随后删除两个约束，为三条声明通道写入精确样本 key，并统一设置 linear in/out tangent。事务后从实际动画曲线读取 source kind、key 时间、值和切线逐项复检；Body、源动画、当前帧和用户选择不被改写。一次 Undo 恢复实时驱动及约束并移除 bake 曲线，原 Body 动画继续存在。

242 项纯 Python 全量测试通过。Maya 2024 隐藏独立进程 PID 8212 在自行生成、带 provenance 的 Z-Up Body 上建立三轴位移和旋转动画，再把 1–5 帧实时 Root Motion 逐帧物化。三个导出通道各生成 5 个 linear key，实际回放与采样值一致；约束移除、currentTime/选择/Body 保持、一次 Undo 恢复实时驱动、第二次 Undo 移除 Root Motion、场景清理和进程退出全部通过。

本切片不提供关键帧降采样、曲线简化、Euler filter、片段拼接、完整导出骨架或 FBX 文件写入。下一步继续在 Maya 内定义显式导出集合与骨架边界，然后再接文件输出；Blender 第二阶段保持冻结。

## Maya 切片 83：Root Motion 下的完整 Export Skeleton

本机清单只用于确认 Root Motion 与 FBX 选择导出相邻，没有读取、翻译或派生导出重命名、层级重组或 FBX 选项实现。本切片定义独立的 `BodyExportSkeletonPlan`：owned Body 的每个 joint 都映射为带 `AdvPy_EXP_` 前缀的 export joint，保持源拓扑、jointOrient、side 和 label；export root 是 Root Motion 的唯一骨架子根。角色有显式 namespace 时，输出留在同一 namespace。当前要求 Body joint 短名唯一，不猜测重名层级的游戏端命名策略。

根 export joint 通过 parent constraint 在 Root Motion 父空间内保持源 Body root 的世界姿态，scale 三轴直接跟随源 root；其余 export joints 直接连接对应 Body joint 的局部 translate、rotate 和 scale。每个输出 joint 另有只读 source message，export root 写入 owner、artifact kind、schema、源 Body root 和 joint 数量 provenance。应用层同时预检 owned Body、实时 Root Motion、全部输出/约束名称和输入快照；一个 Undo Chunk 创建完整层级并从实际 DAG、世界姿态、来源 plugs、标签和 provenance 复检。该层级存在时 Body ReBuild 会因明确的外部依赖停止，Undo 移除后恢复安全。

246 项纯 Python 全量测试通过。首次两次隐藏 Maya 验证在构建后审计阶段正确回滚，定位到 Maya parentConstraint 是 export root 下的 DAG constraint，incoming plug 因而使用完整 DAG 路径；核心预期改为真实完整路径后，Maya 2024 隐藏进程 PID 39884 验证通过。自行生成的 30 关节 Body 完整生成 30 个 export joints，Root/Shoulder 复合运动下每个输出 joint 的世界位置和轴都实时匹配源 Body。选择、provenance、ReBuild 阻断、一次 Undo 只移除 export skeleton、第二次 Undo 移除 Root Motion、Body/Fit 保留和场景清理全部通过。

本切片仍是实时导出层，不 bake export joints、不删除 source message、不创建 Maya objectSet，也不写 FBX 文件。下一步将 Root Motion 与整棵 export skeleton 在同一显式帧范围内物化并断开 Body 依赖，形成可独立选择导出的动画层级；Blender 第二阶段继续冻结。

## Maya 切片 84：独立动画 Export Skeleton bake

`BodyExportSkeletonBakePlan` 复用已经验证的整数帧范围规则，把实时 Root Motion 和完整 Export Skeleton 视为同一个动画产物。每帧样本同时记录 Root Motion 的平面位移/yaw，以及每个 export joint 的本地 translate、rotate、scale；joint 路径集合和顺序必须与实时 export 计划完全一致。核心审计要求 Root Motion 三通道、每个 joint 九通道都由独立 animation curve 驱动，key 时间和值等于样本，in/out tangent 为 linear，并核对 bake schema、起止帧和采样步长。

`BakeBodyExportSkeleton` 在事务前复检 owned Body、实时 Root Motion、实时 Export Skeleton 和 provenance。Maya adapter 在一个 Undo Chunk 内完成采样与 currentTime 恢复，删除 Root Motion point/orient constraints 和 export root parent constraint，断开所有 Body→Export TRS 输入，删除每个 joint 的 source message，再写入完整关键帧与根级 bake 元数据。完成后输出层级只保留自身动画曲线和内部父子关系；Body 动画继续存在，选择和当前帧保持。一次 Undo 会删除全部 bake 曲线/元数据并恢复约束、TRS 接线和 source message。

249 项纯 Python 全量测试通过。Maya 2024 隐藏独立进程 PID 47568 在自行生成的 30 关节 Body 上建立 Root 与 Shoulder 动画，按 1–5 帧逐帧 bake。Root Motion 三通道与 30×9 个 joint TRS 通道全部得到 5 个 linear keys，所有实时约束和 source message 消失，Body 与输出层级之间跨依赖为零；五个整数帧回放中每个 export joint 的世界位置和轴都匹配源 Body。源动画、currentTime、选择和 Body 保持，一次 Undo 恢复完整实时导出层，后续 Undo 分别移除 Export Skeleton 与 Root Motion，场景和进程清理通过。

该输出现在可以作为独立动画骨架选择范围，但本切片不创建 Maya objectSet、不执行 Euler filter/曲线简化，也不加载 FBX 插件或写文件。下一步定义只包含 Root Motion 层级的显式 export selection contract，再以临时文件、拒绝覆盖和落盘复检接入 FBX；Blender 第二阶段保持冻结。

## Maya 切片 85：独立 Export Skeleton 的安全 FBX 发布

`BodyFbxExportSelection` 把文件边界固定为 Root Motion 加完整 Export Skeleton，不依赖当前选择，也不要求先在场景中创建持久 objectSet。导出前的纯核心审计只接受本工程 provenance 与调用方帧范围完全一致的 bake：Root Motion 必须仅保留三条独立动画曲线，30 个 export joints 必须各有九条独立 TRS 曲线，全部关键帧覆盖明确采样序列且使用 linear 切线，实时约束、source message 和 Body 跨连接必须为零。该审计只判断结构证据，不用重新读取的值与自身比较来制造虚假的正确性。

`ExportBodyFbx` 要求绝对 `.fbx` 新路径，拒绝不存在或符号链接目录、已有目标和场景执行前漂移。Maya adapter 静默加载随 Maya 提供的 `fbxmaya`，保存并恢复 FBX exporter 设置、用户选择、当前帧和场景 modified 状态；它关闭 camera/light/shape/skin/constraint 与输入连接扩展，只对显式 31 个 joint 执行 selection export。文件先写到目标同目录的唯一临时路径；只有在确认 FBX binary/ASCII 文件头、非空大小与 SHA-256 后，才通过不覆盖的同卷原子发布暴露目标，失败只清理本次拥有的临时文件。

253 项纯 Python 全量测试通过。Maya 2024 隐藏独立进程 PID 33520 使用自行生成的 30 关节 Body 与 1–5 帧 Root/Shoulder 动画，导出 279040 字节 binary FBX；实时层级的 Body 连接被审计捕获，bake 后降为零。新场景重新导入后恰有一个根级 Root Motion 和 30 个 export joints，两类测试通道均保留 1–5 帧关键帧，FitSkeleton、源 `Root_M` 与控制器没有泄漏。选择、currentTime、modified 状态、拒绝覆盖、FBX 插件版本记录、临时目录清理和进程自行退出全部通过。

本切片不创建持久 Maya objectSet，不做 Euler filter、曲线简化、坐标轴/单位预设、蒙皮与网格导出，也不定义 Unreal/Unity 特定约定。下一步仍在 Maya 第一阶段内把导出参数与命名语义继续收敛；Blender 第二阶段保持冻结。

## Maya 切片 86：FBX 规范发布名称与场景无损恢复

本轮只从本机安全清单读取过程名称、行数和调用边，确认导出改名、名称恢复与选择导出是相邻边界；没有读取或翻译这些过程体。核心新增 `BodyFbxPublishedNode`，把 Maya 场景路径与文件发布路径分开：场景继续用 namespace 和 `AdvPy_EXP_` 避免多角色冲突，默认 FBX 根发布为 `RootMotion`，其余 30 个 joint 恢复为来源 Body 的无 namespace 短名。发布名限定为稳定 ASCII 标识符，完整父链由已有 export 拓扑确定，重复路径和非法根名在宿主操作前拒绝。

应用计划同时记录发布路径冲突；`|RootMotion` 或任何实际发布路径被场景中其他节点占用时，不加载文件写入事务。Maya adapter 在一个专用 Undo Chunk 内先移除 export root 的内部 provenance/bake 属性，再按叶到根顺序临时改名并复检全部 31 个路径，随后只导出该规范路径集合。无论 FBX 成功或失败，这个事务都会立即执行一次 Undo；原始 joint 路径、锁定元数据、选择、currentTime、modified 状态和原有 Undo 队列顶部都必须恢复，应用层只在宿主恢复完成并验证临时 FBX 后发布最终文件。

256 项纯 Python 全量测试通过，新增覆盖规范父链、namespace/开发前缀剥离、非法发布名和 `|RootMotion` 冲突。Maya 2024 隐藏独立进程 PID 43016 导出 275568 字节 binary FBX：原场景 30 个 `AdvPy_EXP_` joints、Root Motion、bake 元数据和 Undo 顶部全部恢复；新场景回读得到 `RootMotion → Root_M → ...` 的 31-joint 动画层级，不含 `AdvPy_EXP_`、namespace、Fit/Body/control 节点或本工程内部属性，1–5 帧 Root/Shoulder 动画保持。进程自行退出且未留下 Maya/mayapy。

本切片提供通用规范命名，不实现 Unreal Mannequin、MetaHuman、Mixamo 等专用骨名表，也不做单位/Up Axis 转换、Euler filter、曲线简化、蒙皮或网格导出。下一步继续在 Maya 第一阶段内定义显式 FBX 文件版本、单位和坐标配置；Blender 第二阶段保持冻结。

## Maya 切片 87：显式 FBX Profile 与文件头复检

本轮继续只读取本机安全清单中的过程名称、行数和调用边，没有读取或派生 ADV 过程体。核心新增 DCC 无关的 `BodyFbxExportProfile`，显式表达 FBX2018/FBX2020、Y/Z Up、centimeter/meter 与 binary/ASCII；默认 Profile 保持当前 Maya 场景的 Up Axis 和线性单位，避免无意转换。单位比例按源场景单位与目标文件单位共同计算，因此 cm→m 为 100、m→m 为 1。应用计划冻结源单位与 Profile，执行前拒绝场景轴或单位漂移，并同时审计 Maya exporter 的版本、轴、比例、编码回读和最终文件头。

Maya adapter 仍在 `FBXPushSettings`/`FBXPopSettings` 边界内应用参数，不把用户 exporter 偏好永久改写。binary 文件从标准头读取 32 位格式版本，ASCII 文件从头部 `FBXVersion` 读取版本；FBX2018 必须对应 7500，FBX2020 必须对应 7700，编码或版本不一致时目标文件不会发布。当前单位合同只接受 Maya `cm`/`m`，其他场景单位会在导出前明确拒绝，而不是猜测比例。

258 项纯 Python 全量测试通过，新增覆盖默认 cm 场景、默认 m 场景的 1.0 比例、显式 cm→m 的 100 比例、FBX2018 ASCII 文件头与既有拒绝覆盖边界。Maya 2024 隐藏独立进程 PID 38760 在一次运行中写出 275472 字节 FBX2020/Z-Up/cm/binary 和 382734 字节 FBX2018/Y-Up/m/ascii；两套 Profile 均通过 exporter 回读和文件头审计，显式转换文件重新导入后仍得到规范 `RootMotion → Root_M → ...` 的 31-joint 动画层级。选择、currentTime、modified 状态、Undo 顶部、临时发布名称和内部元数据全部恢复，进程自行退出且没有 Maya/mayapy 残留。

本切片不增加 millimeter/foot 等单位、自动引擎 Profile、Unreal/MetaHuman/Mixamo 骨名表、Euler filter、曲线简化、蒙皮、网格或 UI。下一步继续 Maya 第一阶段的导出合同；Blender 第二阶段保持冻结。

## Maya 切片 88：MoCap 来源骨架只读检查

安全清单只用于确认 `asMoCapMatcherNameSpaceDetect`、`asMoCapMatcherScan`、disconnect 与 bake 是相邻但可分离的边界；本轮未读取或翻译这些过程体，也未读取 matcher 文件正文。核心新增 `MocapSourceSnapshot`：每个关节只记录宿主路径、路径无关名称、namespace 和 joint 父级，每个受支持 TR 通道记录通用 driver kind、来源标识与关键帧时间。纯 Python 审计要求显式根、连续 joint 父链、统一 namespace、唯一可移植名和至少一条直接 animation curve；混合 namespace、非 joint 断层、重复名、非有限/乱序关键帧及 constraint/DG 输入都会成为明确问题。

application 层 `InspectMocapSource` 只捕获一次场景并返回检查结果，调用方可先展示全部问题，再用 `require_valid()` 获得 namespace、关节/动画通道数量、动画关节数量、浮点 start/end 和有序可移植关节名。Maya 读取被隔离到独立 `MayaMocapSourceReader`，不依赖 Body 构建适配器，不读取当前选择，也不创建无意义的 Undo Chunk。

262 项纯 Python 全量测试通过。Maya 2024 隐藏独立进程 PID 50584 检查自行生成的 `TakeA:Hips → Spine → Chest → Neck → Head`：3 条直接动画曲线覆盖 2 个关节和 1–20 帧，namespace 与五个路径无关名称正确；接入一个 `multiplyDivide` 驱动后只读预检识别为不支持的非曲线来源。两次检查均保持节点、选择、currentTime、modified 状态与 Undo 顶部，清理后没有 Maya/mayapy 残留。

本切片不导入外部 FBX，不猜测 Mixamo/Plask/Unreal 等厂商命名，不创建映射、constraint、Control Rig、重定向或 bake，也不增加 UI 或 Blender 实现。下一步在 Maya 第一阶段定义调用方显式提供、可审计的 MoCap→Body 关节映射合同。

## Maya 切片 89：显式 MoCap→owned Body 映射

本轮安全清单仍只读取 matcher scan/disconnect/bake 的过程名称、行数和调用边，没有读取实现体。核心 `MocapJointMapping` 由调用方明确给出路径无关的 source/target 名称以及平移、旋转策略；不含 Mixamo、Plask、Unreal 或任何其他厂商表。审计先复用 MoCap 来源结构检查，再要求目标 Body provenance 与指定 Fit 来源和关节数量一致。来源与目标都必须唯一且一一对应；MoCap 根必须映射到 Body 根并传递平移与旋转，非根映射不得传平移。

为了让部分映射仍保持骨架语义，核心分别沿 MoCap 和 Body 父链寻找最近的已映射祖先，要求两边对应关系一致；即使中间 Spine/Chest 层未映射，也允许跨层对应，但反转 Head/Spine 等层级会在宿主修改前报告。`MocapBodyMappingPlan` 只保存已解析 source/target 路径、传递策略、来源关节实际动画属性、namespace 与 clip 范围。application 用例各捕获一次来源和 Body；`MayaMocapMappingReader` 组合独立 MoCap reader 与既有 canonical Body reader，不复制 Body 捕获逻辑。

266 项纯 Python 全量测试通过。Maya 2024 最终隐藏独立进程 PID 32444 在自行生成的 30 关节 owned Body 与 `TakeA` 五关节动画源之间解析 5 项显式映射：根路径、1–20 帧、3 条实际动画属性和 namespace 均正确；交换 Spine/Head 目标后拓扑审计明确拒绝。检查前后节点、选择、currentTime、modified 状态和 Undo 顶部不变，清理后没有 Maya/mayapy 残留。首次合成验证因脚本漏设既有 Body 素材所需的 Z-Up 而在 Body 朝向复检阶段回滚，映射代码尚未执行；补齐素材前置条件后通过。

本切片不创建 constraint、不改变 Body、不执行 retarget 或 bake，也不导入外部文件、不提供厂商自动映射、映射 JSON、UI 或 Blender 实现。下一步在 Maya 第一阶段让已验证计划建立可撤销的临时驱动层，并明确连接后的结构复检与断开边界。

## Maya 切片 90：可审计的 MoCap 临时驱动与显式断开

本切片只消费切片 89 已验证的显式映射，不读取或翻译 ADV matcher/disconnect 过程体。DCC 无关的 `MocapBodyConnectionPlan` 为根映射定义 parent constraint，为非根映射定义 orient constraint；约束名由目标 Body namespace 与路径无关目标名确定，根占用六个 TR 输入，非根只占用三个 rotate 输入。它不写关键帧、不计算厂商预设，也不把 Maya API 放进核心。

`ConnectMocapBody` 在修改前拒绝同名节点、已有通道输入和二次快照漂移，并保存全部映射目标的世界矩阵。Maya 在一个 Undo Chunk 内以 maintain-offset 创建约束，随后从真实 constraint target、output 和 Body incoming plug 逐项复检；任何结构错误或当前姿态变化都会撤销整个连接。`DisconnectMocapBody` 先证明同名节点的类型、source、driven target 和每条输出都与计划完全一致，只删除这组已证明归属的节点，并复检目标输入全部释放；遭到改名、改接或同名异类节点污染时拒绝删除。

272 项纯 Python 全量测试通过。Maya 2024 隐藏独立进程 PID 10636 在自行生成的 30 关节 Z-Up owned Body 与 `TakeA` 五关节动画源之间创建 1 个 parent constraint 和 4 个 orient constraints：连接瞬间 Body 世界姿态保持，切到第 20 帧后 Body 确实响应源动画；断开后约束和输入消失，一次 Undo 恢复断开、再一次 Undo 完整移除连接。来源动画、选择、业务操作前后的 currentTime、场景清理与进程退出全部通过。首次真实连接验证发现 Maya constraint 是目标 joint 下的 DAG 节点，适配器据真实长路径读取，但核心所有权标识继续使用确定性短名；第二次失败仅来自验证脚本把切帧写进 Undo 队列，关闭该测试切帧的 Undo 记录后业务 Undo 边界通过。

本切片不 bake、不导入 FBX、不创建 Control Rig、不保存映射预设、不自动猜测 Mixamo/Plask/Unreal 命名，也不增加 UI 或 Blender 实现。下一步继续 Maya 第一阶段，为这一临时驱动层定义显式采样与 bake 边界；Blender 第二阶段保持冻结。

## 不能依赖纯自动转换的部分

- MEL 全局变量跨过程共享，Python 模块命名空间不会自动等价。
- 字符串拼接的 `eval`、`evalDeferred`、按钮回调需要改成显式 callable。
- MEL 空数组、越界填充、命令返回值与 `maya.cmds` 的 `None` 行为不同。
- `catch/catchQuiet`、隐式选择集、当前父 UI 和 Maya 全局状态都需要明确建模。
- 过程之间存在大量按名字调用，机械分文件不会自动得到合理模块边界。

## 每次迁移的轻量验收

每个切片只保留三层证据：纯 Python 单元测试、同一输入场景的旧/新状态快照对比、一次独立 Maya GUI 生命周期烟雾测试。不会为每个过程建立重量级门禁；高风险构建与文件写入才增加专门场景。
