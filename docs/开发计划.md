# 开发计划

更新日期：2026-09-26

## 当前结论

工程尚未完成 AdvancedSkeleton 的全量复刻，也没有足够证据声称原版源码行为已全部重建。

当前成果是一个可运行、可测试的 Maya Python 绑定框架：Fit 数据、基础 Body、四肢与手部控制、可变脊柱、蒙皮、姿态与动画、MoCap、FBX、基础 Face 工作流均已有纵向实现和自生成场景验证。界面已按 AdvancedSkeleton 6.925 的栏目顺序组织，但大部分原版栏目仍没有对应操作；生产资产覆盖、完整 FaceSetup、原版逐项行为对照和 Blender 正式迁移也未完成。

在建立可审计的原版功能清单前，不给出整体完成百分比。现有测试数量只能说明已经实现部分的稳定性，不能代表原版功能覆盖率。

## 完成标准

“全量复刻完成”必须同时满足以下条件：

1. 建立原版功能覆盖矩阵，每个公开操作都有“已实现、明确不实现、待实现”三者之一的结论。
2. 已实现操作覆盖输入、结果、失败条件、撤销／重做、保存重开和场景所有权边界。
3. Maya 可见界面中的入口、参数和反馈能完成对应工作流，不依赖开发命令补齐步骤。
4. 至少使用一套公开示例角色和多套自建差异角色完成 Fit → Body → 控制 → Skin → Animate → Export 全链路。
5. 生产常见的命名空间、引用、动画曲线、多个 Skin、外部驱动和重建场景通过验收。
6. 文档准确区分已实现能力、已验证范围和仍未覆盖的情形。

## 当前覆盖

| 模块 | 当前状态 | 已有能力 | 主要缺口 |
| --- | --- | --- | --- |
| 分层架构与事务 | 基线完成 | Core / Application / Adapter 分层；预检、单次撤销事务、写后读回 | 需要随新增功能持续约束，不能单独代表产品完成 |
| Fit 与 Body | 可用基线 | Fit 导入导出、30／70 关节 Body、可变脊柱角色构建与登记 | 原版所有 Fit 选项、身体变体、关节编辑工具和生产角色覆盖 |
| 四肢、手、躯干控制 | 可用基线 | FK/IK、匹配、拉伸、扭转、reverse-foot、手指控制、脊柱 IK/FK | 原版全部控制选项、自定义控制器、局部关节和更复杂驱动组合 |
| 控制空间与动画 | 部分完成 | 当前帧与指定帧切换、姿态和片段、四肢／脊柱区间转换 | 完整带键空间历史保持、动画层、复杂曲线和连续子帧等价 |
| Skin 与变形 | 部分完成 | 显式绑定、权重 JSON、镜像、跨拓扑表面转移、多 Skin 场景 | DeltaMush、Shapers、Muscular 等原版变形栏目；广泛生产拓扑和变形栈 |
| MoCap 与 FBX | 部分完成 | 显式映射、控制写入、独立导出骨架、Root Motion、FBX Profile | 自动映射、更多外部 FBX 变体、动画层、非标准曲线和长期素材兼容 |
| Face | 早期可用 | 目标生成、基础控制、BlendShape 动画、资产版本往返 | 自动解剖标记、表面配准、眼睑／下颌骨骼、组合规则、皱纹与完整 FaceSetup |
| Maya 界面 | 框架完成、入口不完整 | 原版栏目顺序、已迁移操作的中文参数页 | 大量栏目显示“尚未实现”；原版控件逐项对应和完整可见会话验收 |
| Blender | 尚未开始正式迁移 | 早期适配器可行性代码 | 第二阶段全部产品功能 |
| 原版行为覆盖审计 | 未完成 | 已知公开栏目结构和当前实现清单 | 缺少逐操作行为矩阵，因此不能证明“源码全部复刻” |

## 开发顺序

### P0：建立可度量的覆盖基线

- 已建立 [功能覆盖矩阵](功能覆盖矩阵.md)：49 个末级栏目均有唯一编号和当前入口状态；Preparation、Body / Fit、Edit、Build、Deform、Geometry、Control Curves、Control Orient、Body／Face Custom Controllers、Pose、Tools、Publish、Export 已拆到操作层。尚未实现的操作保留待实现状态。
- 从原版公开界面、帮助文档和合法可用的本机安装行为整理功能矩阵。
- 为每个操作记录输入、场景变化、输出、失败条件及当前实现入口。
- 将“按钮存在”“后台用例存在”“生产工作流通过”分开计数。

完成条件：所有顶层栏目和公开子操作都有唯一编号与状态，可以据此计算真实行为覆盖率。当前完成栏目层及上述操作层；Face 其余栏目和其他工具仍需拆分，因此不计算整体行为覆盖率。已纠正 Deform 栏目误映射：SkinCage、SubWrap、SkinLoops 均未复刻，通用蒙皮、权重 JSON 和跨拓扑转移只是相关扩展。

DEFORM-01～02 已接入原版 Skinning 按钮：按角色 DeformSet 追加选择变形关节并保留所选网格；没有 DeformSet 时使用已登记 Body 关节；写入原版 9 项 Smooth Bind 选项并在图形会话打开 Maya 原生绑定窗口。Maya 2024 后台验证了选择集和选项读回，面板离屏加载通过；原生窗口与公开角色的可见操作待验。Deform option2／3 的旧导航误将权重 JSON／跨拓扑转移显示为 SkinCage／SubWrap，现已撤下，工具仍保留在蒙皮详情页。

### P1：完成 Maya Body 主链路

- 用公开示例角色完成 Fit、Body、控制构建、蒙皮、姿态、动画和导出。
- 补齐主链路中仍需命令行或手工拼接的步骤。
- 覆盖重建、命名空间、引用、撤销／重做和保存重开。

完成条件：用户只通过 Maya 界面即可从标准 Fit 得到可动画、可导出的完整角色；输出场景可独立重开。

### P2：补齐高频 Body 与 Deform 功能

- 按覆盖矩阵优先实现高频身体编辑、自定义控制器、局部关节和变形选项。
- 为每一组连贯工作流增加对应的宿主验收，不按单个函数反复测试。
- 使用不同关节数、比例、命名空间和两类以上网格验证。

完成条件：高频 Body／Deform 操作全部有界面入口和真实 Maya 结果，失败时不留下半成品。

### P3：完成动画、MoCap 与发布兼容

- 实现带键控制空间切换的历史动画保持。
- 扩大 FBX、帧率、单位、旋转顺序、动画层和外部曲线覆盖。
- 补充自动映射与可保存映射预设。

完成条件：多段现有动画在模式与空间转换后保持可接受误差；外部输入和发布结果可重复验证。

### P4：完成 Face 工作流

- 建立面部标记、求解、控制、变形器和资产版本的完整数据合同。
- 实现眼睑、下颌、表情组合和复杂目标管理。
- 使用公开或自建的不同面部拓扑验证。

完成条件：从面部准备到表情动画可通过界面完成，重建和版本刷新不会破坏已有动画。

### P5：收口界面与生产验收

- 移除主工作流中的“尚未实现”入口，或在覆盖矩阵中明确排除原因。
- 对照原版检查栏目排布、操作入口、参数默认值和状态反馈。
- 录制并保存一套完整可见 Maya 验收证据。

完成条件：覆盖矩阵中计划支持的 Maya 项全部关闭，没有依赖隐藏开发入口的步骤。

### P6：启动 Blender 第二阶段

只迁移已在 Maya 中稳定的数据合同和用例语义。Maya 主线未达到 P5 前，不以 Blender 原型增加整体完成度。

## 开发与验证节奏

每轮先完成一个可操作的纵向工作包，再集中验证：

1. 完成 Core、Application、Maya Adapter 和界面入口。
2. 运行该工作包的纯 Python 回归和一个真实 Maya 场景验收。
3. 在里程碑结束时运行完整回归、架构边界检查和关键宿主场景。
4. 只保留脚本、固定夹具和结论；临时场景、截图、日志与调试探针不进入长期工程。

## Codex Goal

完成 AdvancedSkeleton Python 重构的 Maya 主线。以本计划和功能覆盖矩阵为进度基线，连续完成 P1～P5；Maya 验收达标后进入 Blender。每轮只抓一个可由用户操作的原版工作流：先从原版界面和场景确认输入、节点关系、动作结果与失败边界，再贯通 Core、Application、Maya Adapter 和界面入口。写完整个工作包后集中跑相关纯 Python 回归和真实 Maya 验收，不按单个函数频繁跑全套测试。真实场景必须检查控制动作或网格变形、失败回滚、一次撤销／重做、保存重开与角色登记；涉及原版资产时记录同名关节矩阵和顶点误差。对照不通过就定位上游驱动并修复同一工作包，不降低验收门槛。通过后更新覆盖矩阵与计划，提交并推送公开仓库。按钮、测试数量和相似扩展功能都不能代替原版行为覆盖。优先打通公开角色的网格、121 个 Skin 影响关节、原权重和分段驱动，再推进后续栏目。

当前工作包：重建原版公开角色四肢 12 个有权重的 `Part1/Part2` 分段关节。本项目的远端 Body 关节直接挂在近端 Body 下，原版则经过两个 Part 关节；辅助分支据此补偿近端扭转，并把远端局部段长分为三份。原版肩／肘／腕 FK 控制旋转顺序为 `5`，髋／膝为 `2`，踝为 `3`，脚趾为 `5`；新控制和机制关节按相同顺序创建。`sam.mb` 的腿原本处于 IK 模式；原版 FK／IK 属性范围为 `0～10`，本项目为 `0～1`，对照时使用两边相同的实际混合权重。静止姿态、双侧肩／肘／髋控制器的 X／Y／Z 单轴 12° 及混合 `(12°, 7°, -5°)`、自定义 `twistAmount`／`twistAddition` 和 FK 非等比缩放 `(1.2, 0.8, 1.1)` 后，12 个 Part 世界矩阵最大分量误差不超过 `2.6e-7`。双侧 Arm／Leg 全 IK 下，IK 控制器在世界空间上移 `0.3 cm`，Part 最大矩阵误差 `0.00341`；原版腕／膝上游扭转输入已加入对应分段图。一次撤销／重做、保存重开和 74 个 Body 关节登记通过。429 项纯 Python 测试、Arm／Leg FK 机制宿主回归与架构边界检查通过。当前增量只覆盖这套公开角色的取样动作；IK Fatness／体积、更广姿态与命名空间、原权重和动作后的网格顶点尚未验收，不能将原 Skin 迁移记为完成。

当前循环：扩展 Control Orient。CO-05 的自建角色双侧臂腿 FK、ArmIK、LegIK、ToeIK 和 FK 模式肩胛控制通过三轴后台采样；Pole Vector 旋转没有 Body 驱动，只更新方向。肩胛切到 IK 模式后的 Z 轴约有 5% 动作量的朝向误差：肩、肘位置对称，关节朝向未完全镜像，调整控制器正负号或倍率不能消除。手部和公开示例角色未验，CO-05 保持部分完成。CO-06 World Match 已按本机 6.925 MEL 的变形关节 X 轴主导世界轴规则实现，通过自建角色十二类双侧 FK／IK／Pole Vector 及命名空间五指控制的 Maya 改向、Body 静止、Undo／Redo 与重开；六种正负主轴矩阵与 Maya 约束逐项吻合。Mirrored Behavior 组合和原版 `sam.mb` 数值对照待验。“朝向子关节”另列为 CO-X1 扩展。CO-07 World Orient 已按原版记录 X Primary／Z Secondary，并通过自建角色十二类双侧控制和命名空间五指控制的 Maya 后台基线。

原版 `sam.mb` 已以禁止脚本节点执行的方式在 Maya 2024 打开：场景为 Y Up、厘米单位，含 41 个 Fit 关节、294 个总关节、141 个控制曲线 Transform 和 1 个网格。原版 41 个 Fit 关节可被当前对称规划器展开成 74 个 Body 关节；原场景已有同名 Body，且 30 个 Fit 关节缺少 Maya 标签。面板的“兼容导出原版 Fit”选项可从嵌套容器只读生成文档，按名称在文档里补齐缺失标签；在空场景导入后，41 个关节、Root 高度偏移和文档语义一致。导入的新 Fit 可通过完整角色用例一次构建并登记 74 个 Body 关节、Arm／Leg／Torso／Global／双侧 Hand 控制；单次 Undo 移除角色而保留 Fit，Redo 和保存重开恢复。在保留原版 Fit 的隔离副本中，面板的“按关节名补全缺失标签”选项也能只给新 Body 推断标签，不改写原 Fit。原版多 Cup、Jaw、JawEnd，手指命名为 `IndexFinger1`～`IndexFinger4` 等，Ring／Pinky 从 Cup 分支生长；Hand 规划器识别这套命名和父链，生成 30 个手指 FK 控制，逐个通过实际关节驱动和撤销。原版 18,151 顶点网格的静态副本可绑定 74 个 Body 关节；总控平移使网格顶点同步移动，Undo 恢复，临时文件保存重开后 Skin 影响关节保留。右食指控制器第 1～5 帧动画通过保存重开；74 关节发布骨架完成 5 帧烘焙，FBX 重新导入 Maya 后得到 75 个关节（含 Root Motion），食指动作保留。Fit 文档路径解决了旧 Rig 同名冲突，但尚未迁移原网格与原权重；FBX 仅含发布骨架，不含网格。原文件未被写入。

原版网格的 Skin 为 18,151 个顶点、121 个影响关节，逐顶点权重和的最大误差约 `5.7e-13`。其中 53 个影响关节与当前 74 关节 Body 同名，另 68 个辅助／分段关节承载总权重质量的 41.40%（7,514.81 个等效全权重点），主要落在脊柱、肩、髋、肘分段。按同名直接导入会丢失这部分权重；不能把新建 Closest Distance 蒙皮视为原版权重保留。下一步必须保留这些辅助变形关节及其驱动，或完成逐顶点、逐姿态对照的等价重映射。

对原版 `sam.mb` 的只读驱动清查将 68 个有权重的辅助关节分成三组：18 个 `Part1/Part2` 分段关节承载 4,729.15 权重质量；40 个 `AJoint`～`DJoint` 体积关节承载 2,685.66；10 个手指末段 `_50` 关节承载 100。68 个关节均有平移、旋转或缩放输入连接，27 个直接挂在同名 Body 父关节下，其余经辅助层级连接。原版手指 `_50` 的位置跟随末段关节，朝向由 `_00` 辅助关节与末段关节等权约束；脊柱分段关节由 FK／IK 两套中间关节约束驱动。迁移不能只复制关节静态变换：必须重建这三组驱动并对照动作后的顶点位置。清查脚本为 `validation/maya_original_skin_driver_inventory.py`，输出留在本机，不纳入仓库。

分段节点图进一步确认四肢 `Part` 不是现有 Twist 关节的简单改名：原版的平移来自矩阵混合与父关节逆矩阵，旋转叠加分配扭转、自定义附加量和相邻关节的反向扭转，缩放随 FK／IK、体积和 Fatness 混合。脊柱、颈部则走另一套 FK／IK 中间关节约束。因此应按四肢和轴向两族重建，再以姿态采样比较原版顶点，不直接复用现有 `AdvPy_*Twist` 驱动图。

静态网格迁移已有独立内部用例：从原版变形结果复制并去除历史，导出为仅含网格的 Maya ASCII 资产，再在 Fit 文档创建的新角色场景导入。`sam.mb` 的 18,151 个顶点逐点比较最大世界坐标误差为 `7.45e-9`，场景无外部引用，74 个 Body 关节与网格保存重开后均保留。该网格尚未绑定原权重。Maya 文件导入会清空当前可撤销记录，现有用例因此不满足单次 Undo／Redo 的产品验收条件，暂不接入面板；后续需改用可撤销的场景内创建路径，并补完整权重、辅助驱动与材质／UV 验收。

轴向分段的 6 个影响关节（`RootPart1/2_M`、`Spine1Part1/2_M`、`NeckPart1/2_M`）现可在新角色中一次构建。默认姿态与原版同名关节的最大位置误差为 `1.33e-8`；颈部控制旋转后分段关节跟随，单次 Undo／Redo 和保存重开通过。辅助关节带有独立类型标记，Body 捕获只返回原有 74 关节，登记角色仍可解析；未标记的额外关节仍触发拓扑错误。这 6 个关节在原 Skin 上承载约 1,736.14 权重质量，但当前只验证位置和基础约束，未验证原版 FK／IK 下的朝向、缩放与顶点变形，不能计为蒙皮保留完成。

手指末段另有 10 个带原名 `_50` 的影响关节，原版每个 `_50` 与对应 Finger3 关节同属 Finger2 子级；同级 `_00` 是静态 Transform，保持 Finger2 的朝向；`_50` 位置跟随 Finger3，朝向由 `_00` 与 Finger3 等权约束。新实现已按此结构在一次事务中创建 10 个辅助关节。首次原版动作对照发现整个左侧 Body 的世界朝向都有 180° 差异：原版左侧在反射后还翻转本地 X、Y 轴，Z 轴保持一致。修正全身镜像规则后，`sam.mb` Fit 重建场景中 68 个同名 Body 关节的静止世界矩阵最大分量差约 `2.2e-6`；新 Body 另有原版不具备的双侧 Heel、FootSideInner、FootSideOuter 共 6 个关节，不能算入同名对照。十根手指 `_50` 默认位置最大分量差 `0.00291 cm`，控制器 X 轴旋转 12° 后十个世界矩阵最大分量差也小于 `0.003`。辅助关节的 Undo／Redo、保存重开和 74 个 Body 关节登记通过。原权重和动作后的网格顶点尚未迁移与对照，因此不计为蒙皮完成。

## 当前下一步

1. 对照原版 `sam.mb` 的 IK Fatness、体积保持、拉伸与更多组合姿态，补齐四肢 12 个 `Part` 的缩放图；完成命名空间和失败回滚验收。
2. 核对轴向 6 个 `Part` 的 FK／IK 朝向和缩放，构建 40 个 `AJoint`～`DJoint` 体积关节；将原网格迁移改为可撤销的产品路径，保留 UV／材质。导入 121 影响关节原权重后按姿态比较顶点，再接通 Maya 可见全链路。
3. 修复 CO-05 肩胛 IK 模式 Z 轴残差，扩展手部与公开示例角色；对照 CO-06／CO-07 和 Mirrored Behavior，完成可见操作验收。
4. 继续登记 Face 与其他未拆分栏目的公开操作，按功能覆盖矩阵推进 P2～P5；主页展示只使用真实 Maya 绑定截图。
