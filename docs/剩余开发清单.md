# 剩余开发清单

本清单记录完整工程的未完成范围。阶段通过只表示对应功能已有证据，不表示 AdvancedSkeleton 全量能力已经替代。开发顺序保持 Maya-first、Blender-second；先连贯实现一组工作流，再集中运行相关回归和后台验收，不按单文件或小功能发布版本。

## 已有基础

Fit 数据编辑与往返、标准 30 / 70 关节 Body、四肢和五指控制、固定双段脊柱、当前姿态匹配与空间切换、显式蒙皮、权重文档、Root Motion、导出骨架与 FBX、显式 MoCap 映射和 Body bake 已有实现。角色登记、保存重开、全身静态姿态与实际蒙皮恢复已完成独立进程验证。

上述能力受各模块声明的拓扑、归属、动画和连接边界约束；代理网格通过不等于生产资产兼容性已经完成。

## 连续开发顺序

| 顺序 | 工作包 | 完成条件 | 当前状态 |
| --- | --- | --- | --- |
| 1 | 全身动画操作 | 原生动画角色恢复；当前帧全通道写键；范围采样、文档往返、批量写键；保留其他帧键值；一次撤销和失败回滚；重开继续编辑 | 单帧、片段与新进程重开在 30 / 70 关节通过 |
| 2 | 动画中的模式与空间切换 | 已有曲线上的 FK/IK 匹配；明确切换时刻和区间；头部、手脚空间来源可动画化；采样帧身体与蒙皮不跳变；原动画可撤销恢复 | 标准 30 / 70 关节通过模式转换、拉伸 / volume、五组空间事件和保存重开；组合烘焙通过，见动画控制空间文档 |
| 3 | 多角色与命名空间 | 显式角色标识；构建、登记、重开、姿态与动画只作用目标角色；两角色同场互不污染；引用角色有明确可写边界 | 完整角色 namespace 已贯通；同场 30 / 70 关节构建、登记、动画操作互不影响；引用角色可发现但拒绝写入 |
| 4 | 保留数据的角色重建 | 重建前保存蒙皮、动画和用户扩展；定义允许的拓扑变化；恢复并验证结果；失败整体恢复旧角色 | 保留快照、同布局暂存与原曲线 / 蒙皮 / 附件交接已通过；旧控制器附加属性、所有权审计及最终原位替换待开发 |
| 5 | 通用身体模块 | 可变脊柱数量、脊柱伸展和头部瞄准；按明确身体描述组合；不同尺寸和拓扑的控制到蒙皮贯通 | 标准拓扑可用，通用化待开发 |
| 6 | 变形与生产资产 | 现有复杂 Skin 的迁移；非对称与缺失影响处理；BlendShape、DeltaMush、Wrap 的独立合同与重建保存 | 显式基础 Skin 已有；其余待开发与资产验证 |
| 7 | 动捕与导出贯通 | 动捕驱动控制 Rig；映射预设；外部动画导入；引擎骨名与发布配置；动画曲线处理 | 显式 Body 映射 / bake、FBX 已有；扩展待开发 |
| 8 | Face | 拟合数据、面部构建、表情和口型；与身体登记、变形、重建共同工作 | 待开发 |
| 9 | 产品入口与兼容收尾 | 界面只调用 application；角色与预设管理；进度和可恢复错误；清点并替换仍依赖旧实现的流程 | 待开发 |
| 10 | Blender 正式迁移 | 复用已稳定的数据与用例合同，建立骨架、控制、变形、动画和持久化适配器 | 只有早期可行性验证，待 Maya 阶段完成 |

每包以实际运行入口、明确限制和对应验收结果关闭。新发现的缺口归入本表，不以增加切片数量替代功能完成度。完整替代还需要逐项行为覆盖审计；当前没有“全部原工具行为已覆盖”的证据。
